---
source_url: https://docs.lightly.ai/train/stable/pretrain_distill/models/
---

# ModelsÂ¶

Lightly**Train** supports pretraining models from various libraries. See Supported Libraries for a list of supported libraries and models.

The model is specified in the `pretrain` command with the `model` argument:

Python
    
    
    import lightly_train
    
    if __name__ == "__main__":
        lightly_train.pretrain(
            out="out/my_experiment",
            data="my_data_dir",
            model="torchvision/resnet50",
        )
    

Command Line
    
    
    lightly-train pretrain out="out/my_experiment" data="my_data_dir" model="torchvision/resnet50"
    

Model names always follow the pattern `<library name>/<model name>`.

Instead of passing a model name, it is also possible to pass a model instance directly to the `pretrain` function:

Python
    
    
    import lightly_train
    from torchvision.models import resnet50
    
    if __name__ == "__main__":
        model = resnet50()                  # Load the model.
        lightly_train.pretrain(
            out="out/my_experiment",
            data="my_data_dir",
            model=model,                    # Pass the model.
        )
    

## List ModelsÂ¶

The `list_models` command lists all available models. Only models from installed packages are listed.

Python
    
    
    import lightly_train
    
    print(lightly_train.list_models())
    

Command Line
    
    
    lightly-train list_models
    

## Supported LibrariesÂ¶

The following libraries are supported (follow the links to get to the respective docs pages):

  * [DINOv2](dinov2.html#models-dinov2)

  * [DINOv3](dinov3.html#models-dinov3)

  * [Torchvision](torchvision.html#models-torchvision)

  * [TIMM](timm.html#models-timm)

  * [Ultralytics](ultralytics.html#models-ultralytics)

  * [RT-DETR](rtdetr.html#models-rtdetr)

  * [RF-DETR](rfdetr.html#models-rfdetr)

  * [YOLOv12](yolov12.html#models-yolov12)

  * [SuperGradients](supergradients.html#models-supergradients)




## Custom ModelsÂ¶

See [Custom Models](custom_models.html#custom-models) for information on how to pretrain custom models.
  *[*]: Keyword-only parameters separator (PEP 3102)
