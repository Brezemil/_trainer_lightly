---
source_url: https://docs.ultralytics.com/ar/yolov5/quickstart-tutorial
---

[GitHub131.6k](https://github.com/ultralytics/ultralytics)

[](https://github.com/ultralytics/ultralytics/blob/main/docs/en/yolov5/quickstart-tutorial.md)

# Link to this sectionدليل البدء السريع لـ YOLOv5 🚀#

ابدأ رحلتك في العالم الديناميكي لـ [كشف الأشياء](https://www.ultralytics.com/glossary/object-detection) في الوقت الفعلي مع Ultralytics YOLOv5! تم تصميم هذا الدليل ليكون نقطة انطلاق شاملة لعشاق ومحترفي الذكاء الاصطناعي الذين يهدفون إلى إتقان YOLOv5. من الإعداد الأولي إلى [تقنيات التدريب](/ar/modes/train) المتقدمة، نحن نوفر لك كل ما تحتاجه. بنهاية هذا الدليل، ستمتلك المعرفة اللازمة لتنفيذ YOLOv5 في مشاريعك بثقة باستخدام أحدث أساليب [التعلم العميق](https://www.ultralytics.com/glossary/deep-learning-dl). لنشغل المحركات وننطلق في عالم YOLOv5!

## Link to this sectionتثبيت#

استعد للانطلاق عن طريق استنساخ [مستودع YOLOv5](https://github.com/ultralytics/yolov5) وإعداد البيئة. هذا يضمن تثبيت جميع [المتطلبات](https://github.com/ultralytics/yolov5/blob/master/requirements.txt) اللازمة. تأكد من أن لديك [**Python >=3.8.0**](https://www.python.org/) و [**PyTorch >=1.8**](https://pytorch.org/get-started/locally/) جاهزين للإقلاع. هذه الأدوات الأساسية ضرورية لتشغيل YOLOv5 بفعالية.
    
    
    git clone https://github.com/ultralytics/yolov5 # clone repository
    cd yolov5
    pip install -r requirements.txt # install dependencies

## Link to this sectionالاستدلال باستخدام PyTorch Hub#

جرب بساطة الاستدلال عبر [PyTorch Hub](/ar/yolov5/tutorials/pytorch-hub-model-loading) في YOLOv5، حيث يتم تنزيل [النماذج](https://github.com/ultralytics/yolov5/tree/master/models) بسلاسة من أحدث [إصدار](https://github.com/ultralytics/yolov5/releases) لـ YOLOv5. تستفيد هذه الطريقة من قوة [PyTorch](https://www.ultralytics.com/glossary/pytorch) لتحميل النماذج وتنفيذها بسهولة، مما يجعل الحصول على التوقعات أمراً مباشراً.
    
    
    import torch
    
    # Model loading
    model = torch.hub.load("ultralytics/yolov5", "yolov5s")  # Can be 'yolov5n' - 'yolov5x6', or 'custom'
    
    # Inference on images
    img = "https://ultralytics.com/images/zidane.jpg"  # Can be a file, Path, PIL, OpenCV, numpy, or list of images
    
    # Run inference
    results = model(img)
    
    # Display results
    results.print()  # Other options: .show(), .save(), .crop(), .pandas(), etc. Explore these in the Predict mode documentation.

## Link to this sectionالاستدلال باستخدام detect.py#

استخدم `detect.py` لعمليات [استدلال](/ar/modes/predict) مرنة على مصادر متنوعة. يقوم البرنامج تلقائياً بجلب [النماذج](https://github.com/ultralytics/yolov5/tree/master/models) من أحدث [إصدار](https://github.com/ultralytics/yolov5/releases) لـ YOLOv5 ويحفظ النتائج بسهولة. هذا النص البرمجي مثالي للاستخدام عبر سطر الأوامر ودمج YOLOv5 في أنظمة أكبر، ويدعم مدخلات مثل الصور، ومقاطع الفيديو، والمجلدات، وكاميرات الويب، وحتى [البث المباشر](https://en.wikipedia.org/wiki/Streaming_media).
    
    
    python detect.py --weights yolov5s.pt --source 0                              # webcam
    python detect.py --weights yolov5s.pt --source image.jpg                      # image
    python detect.py --weights yolov5s.pt --source video.mp4                      # video
    python detect.py --weights yolov5s.pt --source screen                         # screenshot
    python detect.py --weights yolov5s.pt --source path/                          # directory
    python detect.py --weights yolov5s.pt --source list.txt                       # list of images
    python detect.py --weights yolov5s.pt --source list.streams                   # list of streams
    python detect.py --weights yolov5s.pt --source 'path/*.jpg'                   # glob pattern
    python detect.py --weights yolov5s.pt --source 'https://youtu.be/LNwODJXcvt4' # YouTube video
    python detect.py --weights yolov5s.pt --source 'rtsp://example.com/media.mp4' # RTSP, RTMP, HTTP stream

## Link to this sectionالتدريب#

قم بتكرار معايير [مجموعة بيانات COCO](https://cocodataset.org/#home) الخاصة بـ YOLOv5 باتباع [تعليمات التدريب](/ar/modes/train) أدناه. يتم جلب [النماذج](https://github.com/ultralytics/yolov5/tree/master/models) و[مجموعات البيانات](/ar/datasets/detect/coco) الضرورية (مثل `coco128.yaml` أو `coco.yaml` الكاملة) مباشرة من أحدث [إصدار](https://github.com/ultralytics/yolov5/releases) لـ YOLOv5. عادةً ما يستغرق تدريب YOLOv5n/s/m/l/x على [GPU](https://www.ultralytics.com/glossary/gpu-graphics-processing-unit) من نوع V100 حوالي 1/2/4/6/8 أيام على التوالي (لاحظ أن إعدادات [التدريب متعدد GPU](/ar/yolov5/tutorials/multi-gpu-training) تعمل بشكل أسرع). حقق أقصى قدر من الأداء باستخدام أعلى `--batch-size` ممكن أو استخدم `--batch-size -1` لميزة [AutoBatch](https://github.com/ultralytics/yolov5/pull/5092) في YOLOv5، التي تجد تلقائياً [حجم الدفعة](https://www.ultralytics.com/glossary/batch-size) الأمثل. أحجام الدفعات التالية مثالية لـ GPUs من نوع V100-16GB. ارجع إلى [دليل التكوين](/ar/usage/cfg) الخاص بنا للحصول على تفاصيل حول ملفات تكوين النماذج (`*.yaml`).
    
    
    # Train YOLOv5n on COCO128 for 3 epochs
    python train.py --data coco128.yaml --epochs 3 --weights yolov5n.pt --batch-size 128
    
    # Train YOLOv5s on COCO for 300 epochs
    python train.py --data coco.yaml --epochs 300 --weights '' --cfg yolov5s.yaml --batch-size 64
    
    # Train YOLOv5m on COCO for 300 epochs
    python train.py --data coco.yaml --epochs 300 --weights '' --cfg yolov5m.yaml --batch-size 40
    
    # Train YOLOv5l on COCO for 300 epochs
    python train.py --data coco.yaml --epochs 300 --weights '' --cfg yolov5l.yaml --batch-size 24
    
    # Train YOLOv5x on COCO for 300 epochs
    python train.py --data coco.yaml --epochs 300 --weights '' --cfg yolov5x.yaml --batch-size 16

![YOLOv5 training curves for COCO dataset](https://cdn.jsdelivr.net/gh/ultralytics/assets@main/docs/yolov5-training-curves.avif)

ختاماً، لا يُعد YOLOv5 أداة متطورة لكشف الأشياء فحسب، بل هو أيضاً شهادة على قوة [التعلم الآلي](https://www.ultralytics.com/glossary/machine-learning-ml) في تغيير طريقة تفاعلنا مع العالم من خلال الفهم البصري. بينما تتقدم في هذا الدليل وتبدأ في تطبيق YOLOv5 على مشاريعك، تذكر أنك في طليعة ثورة تكنولوجية، وقادر على تحقيق إنجازات رائعة في [الرؤية الحاسوبية](https://www.ultralytics.com/glossary/computer-vision-cv). إذا كنت بحاجة إلى مزيد من الرؤى أو الدعم من زملائك من أصحاب الرؤى، فأنت مدعو إلى [مستودع GitHub](https://github.com/ultralytics/yolov5) الخاص بنا، وهو موطن لمجتمع مزدهر من المطورين والباحثين. استكشف موارد إضافية مثل [منصة Ultralytics](https://platform.ultralytics.com) لإدارة مجموعات البيانات وتدريب النماذج بدون برمجة، أو تحقق من صفحة [الحلول](https://www.ultralytics.com/solutions) الخاصة بنا للاطلاع على تطبيقات العالم الحقيقي والإلهام. استمر في الاستكشاف، استمر في الابتكار، واستمتع بروائع YOLOv5. كشفاً سعيداً! 🌠🔍

المساهمون

[](https://github.com/ultralytics/ultralytics/blob/main/docs/en/yolov5/quickstart-tutorial.md)

[GLglenn-jocher1](https://github.com/glenn-jocher)

تم الإنشاء قبل 3 أيامتم التحديث قبل 3 أيام

## التعليقات

في هذه الصفحة

استكشف باستخدام الذكاء الاصطناعي
