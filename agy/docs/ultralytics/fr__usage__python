---
source_url: https://docs.ultralytics.com/fr/usage/python
---

[GitHub131.6k](https://github.com/ultralytics/ultralytics)

[](https://github.com/ultralytics/ultralytics/blob/main/docs/en/usage/python.md)

# Link to this sectionUtilisation de Python#

Bienvenue dans la documentation sur l'utilisation de Python avec Ultralytics YOLO ! Ce guide est conçu pour t'aider à intégrer facilement Ultralytics YOLO dans tes projets Python pour la [détection d'objets](https://www.ultralytics.com/glossary/object-detection), la [segmentation d'instances](https://docs.ultralytics.com/tasks/segment), la [segmentation sémantique](https://docs.ultralytics.com/tasks/semantic) et la [classification](https://docs.ultralytics.com/tasks/classify). Ici, tu apprendras à charger et utiliser des modèles pré-entraînés, à entraîner de nouveaux modèles et à effectuer des prédictions sur des images. L'interface Python, simple à utiliser, est une ressource précieuse pour quiconque souhaite incorporer YOLO dans ses projets Python, te permettant d'implémenter rapidement des capacités avancées de détection d'objets. Commençons !

  


  
**Watch:** Mastering Ultralytics YOLO: Python 

Par exemple, tu peux charger un modèle, l'entraîner, évaluer ses performances sur un ensemble de validation et même [l'exporter au format ONNX](/fr/modes/export) avec seulement quelques lignes de code.

Python
    
    
    from ultralytics import YOLO
    
    # Create a new YOLO model from scratch
    model = YOLO("yolo26n.yaml")
    
    # Load a pretrained YOLO model (recommended for training)
    model = YOLO("yolo26n.pt")
    
    # Train the model using the 'coco8.yaml' dataset for 3 epochs
    results = model.train(data="coco8.yaml", epochs=3)
    
    # Evaluate the model's performance on the validation set
    results = model.val()
    
    # Perform object detection on an image using the model
    results = model("https://ultralytics.com/images/bus.jpg")
    
    # Export the model to ONNX format
    success = model.export(format="onnx")

## Link to this sectionEntraîner (Train)#

Le [mode Entraînement (Train)](/fr/modes/train) est utilisé pour entraîner un modèle YOLO sur un jeu de données personnalisé. Dans ce mode, le modèle est entraîné en utilisant le jeu de données et les hyperparamètres spécifiés. Le processus d'entraînement implique l'optimisation des paramètres du modèle afin qu'il puisse prédire avec précision les classes et les emplacements des objets dans une image.

Entraîner (Train)
    
    
    from ultralytics import YOLO
    
    model = YOLO("yolo26n.pt")  # pass any model type
    results = model.train(epochs=5)

[Exemples d'entraînement](/fr/modes/train)

## Link to this sectionValider (Val)#

Le [mode Validation (Val)](/fr/modes/val) est utilisé pour valider un modèle YOLO une fois entraîné. Dans ce mode, le modèle est évalué sur un jeu de validation pour mesurer sa [précision](https://www.ultralytics.com/glossary/accuracy) et sa capacité de généralisation. Ce mode peut être utilisé pour ajuster les hyperparamètres du modèle afin d'améliorer ses performances.

Valider (Val)
    
    
    from ultralytics import YOLO
    
    # Load a YOLO model
    model = YOLO("yolo26n.yaml")
    
    # Train the model
    model.train(data="coco8.yaml", epochs=5)
    
    # Validate on training data
    model.val()

[Exemples de validation](/fr/modes/val)

## Link to this sectionPrédire (Predict)#

Le [mode Prédiction (Predict)](/fr/modes/predict) est utilisé pour effectuer des prédictions en utilisant un modèle YOLO entraîné sur de nouvelles images ou vidéos. Dans ce mode, le modèle est chargé à partir d'un fichier de point de contrôle (checkpoint), et tu peux fournir des images ou des vidéos pour effectuer l'inférence. Le modèle prédit les classes et les emplacements des objets dans les images ou vidéos d'entrée.

Prédire (Predict)
    
    
    import cv2
    from PIL import Image
    
    from ultralytics import YOLO
    
    model = YOLO("model.pt")
    # accepts all formats - image/dir/Path/URL/video/PIL/ndarray. 0 for webcam
    results = model.predict(source="0")
    results = model.predict(source="folder", show=True)  # Display preds. Accepts all YOLO predict arguments
    
    # from PIL
    im1 = Image.open("bus.jpg")
    results = model.predict(source=im1, save=True)  # save plotted images
    
    # from ndarray
    im2 = cv2.imread("bus.jpg")
    results = model.predict(source=im2, save=True, save_txt=True)  # save predictions as labels
    
    # from list of PIL/ndarray
    results = model.predict(source=[im1, im2])

[Exemples de prédiction](/fr/modes/predict)

## Link to this sectionExporter (Export)#

Le [mode Exportation (Export)](/fr/modes/export) est utilisé pour exporter un modèle YOLO vers un format exploitable pour le déploiement. Dans ce mode, le modèle est converti en un format utilisable par d'autres applications logicielles ou appareils matériels. Ce mode est utile lors du déploiement du modèle dans des environnements de production.

Exporter (Export)

Exporter un modèle YOLO officiel vers [ONNX](https://www.ultralytics.com/glossary/onnx-open-neural-network-exchange) avec une taille de lot et une taille d'image dynamiques.
    
    
    from ultralytics import YOLO
    
    model = YOLO("yolo26n.pt")
    model.export(format="onnx", dynamic=True)

[Exemples d'exportation](/fr/modes/export)

## Link to this sectionSuivi#

Le [mode Suivi (Track)](/fr/modes/track) est utilisé pour suivre des objets en temps réel en utilisant un modèle YOLO. Dans ce mode, le modèle est chargé à partir d'un fichier de point de contrôle, et tu peux fournir un flux vidéo en direct pour effectuer le suivi d'objets en temps réel. Ce mode est utile pour des applications telles que les systèmes de surveillance ou les [voitures autonomes](https://www.ultralytics.com/solutions/ai-in-automotive).

Suivi
    
    
    from ultralytics import YOLO
    
    # Load a model
    model = YOLO("yolo26n.pt")  # load an official detection model
    model = YOLO("yolo26n-seg.pt")  # load an official segmentation model
    model = YOLO("path/to/best.pt")  # load a custom model
    
    # Track with the model
    results = model.track(source="https://youtu.be/LNwODJXcvt4", show=True)
    results = model.track(source="https://youtu.be/LNwODJXcvt4", show=True, tracker="bytetrack.yaml")

[Exemples de suivi](/fr/modes/track)

## Link to this sectionBenchmark#

Le [mode Benchmark](/fr/modes/benchmark) est utilisé pour profiler la vitesse et la précision de divers formats d'exportation pour YOLO. Les benchmarks fournissent des informations sur la taille du format exporté, ses métriques `mAP50-95` (pour la détection d'objets et la segmentation) ou `accuracy_top1` (pour la classification), ainsi que le temps d'inférence en millisecondes par image à travers divers formats d'exportation comme ONNX, [OpenVINO](https://docs.ultralytics.com/integrations/openvino), TensorRT et d'autres. Ces informations peuvent t'aider à choisir le format d'exportation optimal pour ton cas d'utilisation spécifique en fonction de tes besoins en termes de vitesse et de précision.

Benchmark

Évaluer un modèle YOLO officiel sur tous les formats d'exportation.
    
    
    from ultralytics.utils.benchmarks import benchmark
    
    # Benchmark
    benchmark(model="yolo26n.pt", data="coco8.yaml", imgsz=640, half=False, device=0)

[Exemples de benchmark](/fr/modes/benchmark)

## Link to this sectionUtilisation des entraîneurs (Trainers)#

La classe de modèle `YOLO` sert de wrapper de haut niveau pour les classes Trainer. Chaque tâche YOLO possède son propre entraîneur, qui hérite de `BaseTrainer`. Cette architecture permet une plus grande flexibilité et une meilleure personnalisation de tes [flux de travail en machine learning](https://docs.ultralytics.com/guides/model-training-tips).

Exemple d'entraîneur de détection
    
    
    from ultralytics.models.yolo.detect import DetectionPredictor, DetectionTrainer, DetectionValidator
    
    # trainer
    trainer = DetectionTrainer(overrides={})
    trainer.train()
    trained_model = trainer.best
    
    # Validator
    val = DetectionValidator(args=...)
    val(model=trained_model)
    
    # predictor
    pred = DetectionPredictor(overrides={})
    pred(source=SOURCE, model=trained_model)
    
    # resume from last weight
    overrides["resume"] = trainer.last
    trainer = DetectionTrainer(overrides=overrides)

Tu peux facilement personnaliser les entraîneurs pour prendre en charge des tâches personnalisées ou explorer des idées de recherche et développement. La conception modulaire d'Ultralytics YOLO te permet d'adapter le framework à tes besoins spécifiques, que tu travailles sur une nouvelle tâche de [vision par ordinateur](https://www.ultralytics.com/glossary/computer-vision-cv) ou que tu affines des modèles existants pour de meilleures performances.

[Tutoriels de personnalisation](/fr/usage/engine)

## Link to this sectionFAQ#

### Link to this sectionComment puis-je intégrer YOLO dans mon projet Python pour la détection d'objets ?#

Intégrer Ultralytics YOLO dans tes projets Python est simple. Tu peux charger un modèle pré-entraîné ou entraîner un nouveau modèle à partir de zéro. Voici comment commencer :
    
    
    from ultralytics import YOLO
    
    # Load a pretrained YOLO model
    model = YOLO("yolo26n.pt")
    
    # Perform object detection on an image
    results = model("https://ultralytics.com/images/bus.jpg")
    
    # Visualize the results
    for result in results:
        result.show()

Consulte des exemples plus détaillés dans notre section [Mode Prédiction](/fr/modes/predict).

### Link to this sectionQuels sont les différents modes disponibles dans YOLO ?#

Ultralytics YOLO propose divers modes pour répondre aux différents flux de travail de [machine learning](https://www.ultralytics.com/glossary/machine-learning-ml). Ceux-ci incluent :

  * **[Train](/fr/modes/train)** : Entraîner un modèle en utilisant des jeux de données personnalisés.
  * **[Val](/fr/modes/val)** : Valider les performances du modèle sur un jeu de validation.
  * **[Predict](/fr/modes/predict)** : Effectuer des prédictions sur de nouvelles images ou flux vidéo.
  * **[Export](/fr/modes/export)** : Exporter des modèles vers divers formats comme ONNX et TensorRT.
  * **[Track](/fr/modes/track)** : Suivi d'objets en temps réel dans des flux vidéo.
  * **[Benchmark](/fr/modes/benchmark)** : Évaluer les performances du modèle selon différentes configurations.



Chaque mode est conçu pour fournir des fonctionnalités complètes pour les différentes étapes du [développement et du déploiement de modèles](https://docs.ultralytics.com/guides/model-deployment-options).

### Link to this sectionComment entraîner un modèle YOLO personnalisé en utilisant mon jeu de données ?#

Pour entraîner un modèle YOLO personnalisé, tu dois spécifier ton jeu de données et d'autres [hyperparamètres](https://www.ultralytics.com/glossary/hyperparameter-tuning). Voici un exemple rapide :
    
    
    from ultralytics import YOLO
    
    # Load the YOLO model
    model = YOLO("yolo26n.yaml")
    
    # Train the model with custom dataset
    model.train(data="path/to/your/dataset.yaml", epochs=10)

Pour plus de détails sur l'entraînement et des liens vers des exemples d'utilisation, visite notre page [Mode Entraînement](/fr/modes/train).

### Link to this sectionComment puis-je exporter des modèles YOLO pour le déploiement ?#

Exporter des modèles YOLO dans un format adapté au déploiement est simple avec la fonction `export`. Par exemple, tu peux exporter un modèle au format ONNX :
    
    
    from ultralytics import YOLO
    
    # Load the YOLO model
    model = YOLO("yolo26n.pt")
    
    # Export the model to ONNX format
    model.export(format="onnx")

Pour diverses options d'exportation, réfère-toi à la documentation du [Mode Exportation](/fr/modes/export).

### Link to this sectionPuis-je valider mon modèle YOLO sur différents jeux de données ?#

Oui, valider des modèles YOLO sur différents jeux de données est possible. Après l'entraînement, tu peux utiliser le mode de validation pour évaluer les performances :
    
    
    from ultralytics import YOLO
    
    # Load a YOLO model
    model = YOLO("yolo26n.yaml")
    
    # Train the model
    model.train(data="coco8.yaml", epochs=5)
    
    # Validate the model on a different dataset
    model.val(data="path/to/separate/data.yaml")

Consulte la page [Mode Validation](/fr/modes/val) pour des exemples détaillés et l'utilisation.

Contributeurs

[](https://github.com/ultralytics/ultralytics/blob/main/docs/en/usage/python.md)

[GLglenn-jocher20](https://github.com/glenn-jocher)[RIRizwanMunawar3](https://github.com/RizwanMunawar)[JKjk4e2](https://github.com/jk4e)[RAraimbekovm1](https://github.com/raimbekovm)[CDcdeil1](https://github.com/cdeil)[LEleonnil1](https://github.com/leonnil)[MAMatthewNoyce1](https://github.com/MatthewNoyce)[IVIvorZhu3311](https://github.com/IvorZhu331)[AYAyushExel1](https://github.com/AyushExel)

Créé 12 nov. 2023Mis à jour la semaine dernière

## Commentaires

Sur cette page

Explorer avec l'IA
