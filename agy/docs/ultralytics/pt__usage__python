---
source_url: https://docs.ultralytics.com/pt/usage/python
---

[GitHub131.6k](https://github.com/ultralytics/ultralytics)

[](https://github.com/ultralytics/ultralytics/blob/main/docs/en/usage/python.md)

# Link to this sectionUso em Python#

Bem-vindo à documentação de Uso em Python do Ultralytics YOLO! Este guia foi projetado para te ajudar a integrar perfeitamente o Ultralytics YOLO em seus projetos Python para [detecção de objetos](https://www.ultralytics.com/glossary/object-detection), [segmentação de instâncias](https://docs.ultralytics.com/tasks/segment), [segmentação semântica](https://docs.ultralytics.com/tasks/semantic) e [classificação](https://docs.ultralytics.com/tasks/classify). Aqui, você aprenderá como carregar e usar modelos pré-treinados, treinar novos modelos e realizar previsões em imagens. A interface Python fácil de usar é um recurso valioso para quem deseja incorporar o YOLO em seus projetos Python, permitindo que você implemente rapidamente capacidades avançadas de detecção de objetos. Vamos começar!

  


  
**Watch:** Mastering Ultralytics YOLO: Python 

Por exemplo, os usuários podem carregar um modelo, treiná-lo, avaliar seu desempenho em um conjunto de validação e até mesmo [exportá-lo para o formato ONNX](/pt/modes/export) com apenas algumas linhas de código.

Python
    
    
    from ultralytics import YOLO
    
    # Create a new YOLO model from scratch
    model = YOLO("yolo26n.yaml")
    
    # Load a pretrained YOLO model (recommended for training)
    model = YOLO("yolo26n.pt")
    
    # Train the model using the 'coco8.yaml' dataset for 3 epochs
    results = model.train(data="coco8.yaml", epochs=3)
    
    # Evaluate the model's performance on the validation set
    results = model.val()
    
    # Perform object detection on an image using the model
    results = model("https://ultralytics.com/images/bus.jpg")
    
    # Export the model to ONNX format
    success = model.export(format="onnx")

## Link to this sectionTreinar#

O [modo Treinamento (Train mode)](/pt/modes/train) é usado para treinar um modelo YOLO em um conjunto de dados personalizado. Nesse modo, o modelo é treinado usando o conjunto de dados e os hiperparâmetros especificados. O processo de treinamento envolve otimizar os parâmetros do modelo para que ele possa prever com precisão as classes e locais dos objetos em uma imagem.

Treinar
    
    
    from ultralytics import YOLO
    
    model = YOLO("yolo26n.pt")  # pass any model type
    results = model.train(epochs=5)

[Exemplos de Treinamento](/pt/modes/train)

## Link to this sectionValidar#

O [modo Validação (Val mode)](/pt/modes/val) é usado para validar um modelo YOLO após ele ter sido treinado. Nesse modo, o modelo é avaliado em um conjunto de validação para medir sua [precisão](https://www.ultralytics.com/glossary/accuracy) e desempenho de generalização. Esse modo pode ser usado para ajustar os hiperparâmetros do modelo a fim de melhorar seu desempenho.

Validar
    
    
    from ultralytics import YOLO
    
    # Load a YOLO model
    model = YOLO("yolo26n.yaml")
    
    # Train the model
    model.train(data="coco8.yaml", epochs=5)
    
    # Validate on training data
    model.val()

[Exemplos de Validação](/pt/modes/val)

## Link to this sectionPrever#

O [modo Previsão (Predict mode)](/pt/modes/predict) é usado para fazer previsões usando um modelo YOLO treinado em novas imagens ou vídeos. Nesse modo, o modelo é carregado a partir de um arquivo de checkpoint, e o usuário pode fornecer imagens ou vídeos para realizar a inferência. O modelo prevê as classes e os locais dos objetos nas imagens ou vídeos de entrada.

Prever
    
    
    import cv2
    from PIL import Image
    
    from ultralytics import YOLO
    
    model = YOLO("model.pt")
    # accepts all formats - image/dir/Path/URL/video/PIL/ndarray. 0 for webcam
    results = model.predict(source="0")
    results = model.predict(source="folder", show=True)  # Display preds. Accepts all YOLO predict arguments
    
    # from PIL
    im1 = Image.open("bus.jpg")
    results = model.predict(source=im1, save=True)  # save plotted images
    
    # from ndarray
    im2 = cv2.imread("bus.jpg")
    results = model.predict(source=im2, save=True, save_txt=True)  # save predictions as labels
    
    # from list of PIL/ndarray
    results = model.predict(source=[im1, im2])

[Exemplos de Previsão](/pt/modes/predict)

## Link to this sectionExportar#

O [modo Exportação (Export mode)](/pt/modes/export) é usado para exportar um modelo YOLO para um formato que pode ser usado para implantação. Nesse modo, o modelo é convertido para um formato que pode ser usado por outras aplicações de software ou dispositivos de hardware. Este modo é útil ao implantar o modelo em ambientes de produção.

Exportar

Exporte um modelo YOLO oficial para [ONNX](https://www.ultralytics.com/glossary/onnx-open-neural-network-exchange) com tamanho de lote (batch-size) e tamanho de imagem dinâmicos.
    
    
    from ultralytics import YOLO
    
    model = YOLO("yolo26n.pt")
    model.export(format="onnx", dynamic=True)

[Exemplos de Exportação](/pt/modes/export)

## Link to this sectionRastrear#

O [modo Rastreamento (Track mode)](/pt/modes/track) é usado para rastrear objetos em tempo real usando um modelo YOLO. Nesse modo, o modelo é carregado a partir de um arquivo de checkpoint, e o usuário pode fornecer um fluxo de vídeo ao vivo para realizar o rastreamento de objetos em tempo real. Este modo é útil para aplicações como sistemas de vigilância ou [carros autônomos](https://www.ultralytics.com/solutions/ai-in-automotive).

Rastrear
    
    
    from ultralytics import YOLO
    
    # Load a model
    model = YOLO("yolo26n.pt")  # load an official detection model
    model = YOLO("yolo26n-seg.pt")  # load an official segmentation model
    model = YOLO("path/to/best.pt")  # load a custom model
    
    # Track with the model
    results = model.track(source="https://youtu.be/LNwODJXcvt4", show=True)
    results = model.track(source="https://youtu.be/LNwODJXcvt4", show=True, tracker="bytetrack.yaml")

[Exemplos de Rastreamento](/pt/modes/track)

## Link to this sectionBenchmark#

O [modo Benchmark (Benchmark mode)](/pt/modes/benchmark) é usado para verificar a velocidade e precisão de vários formatos de exportação para YOLO. Os benchmarks fornecem informações sobre o tamanho do formato exportado, suas métricas `mAP50-95` (para detecção de objetos e segmentação) ou métricas `accuracy_top1` (para classificação), e o tempo de inferência em milissegundos por imagem em vários formatos de exportação como ONNX, [OpenVINO](https://docs.ultralytics.com/integrations/openvino), TensorRT e outros. Essas informações podem ajudar os usuários a escolher o formato de exportação ideal para seu caso de uso específico com base em seus requisitos de velocidade e precisão.

Benchmark

Realize um benchmark de um modelo YOLO oficial em todos os formatos de exportação.
    
    
    from ultralytics.utils.benchmarks import benchmark
    
    # Benchmark
    benchmark(model="yolo26n.pt", data="coco8.yaml", imgsz=640, half=False, device=0)

[Exemplos de Benchmark](/pt/modes/benchmark)

## Link to this sectionUsando Treinadores#

A classe de modelo `YOLO` serve como um wrapper de alto nível para classes Trainer. Cada tarefa YOLO tem seu próprio treinador, que herda de `BaseTrainer`. Essa arquitetura permite maior flexibilidade e personalização em seus [fluxos de trabalho de aprendizado de máquina](https://docs.ultralytics.com/guides/model-training-tips).

Exemplo de Treinador de Detecção
    
    
    from ultralytics.models.yolo.detect import DetectionPredictor, DetectionTrainer, DetectionValidator
    
    # trainer
    trainer = DetectionTrainer(overrides={})
    trainer.train()
    trained_model = trainer.best
    
    # Validator
    val = DetectionValidator(args=...)
    val(model=trained_model)
    
    # predictor
    pred = DetectionPredictor(overrides={})
    pred(source=SOURCE, model=trained_model)
    
    # resume from last weight
    overrides["resume"] = trainer.last
    trainer = DetectionTrainer(overrides=overrides)

Você pode personalizar facilmente os Treinadores para suportar tarefas personalizadas ou explorar ideias de pesquisa e desenvolvimento. O design modular do Ultralytics YOLO permite que você adapte o framework às suas necessidades específicas, esteja você trabalhando em uma nova tarefa de [visão computacional](https://www.ultralytics.com/glossary/computer-vision-cv) ou ajustando modelos existentes para um melhor desempenho.

[Tutoriais de personalização](/pt/usage/engine)

## Link to this sectionFAQ#

### Link to this sectionComo posso integrar o YOLO em meu projeto Python para detecção de objetos?#

Integrar o Ultralytics YOLO em seus projetos Python é simples. Você pode carregar um modelo pré-treinado ou treinar um novo modelo do zero. Veja como começar:
    
    
    from ultralytics import YOLO
    
    # Load a pretrained YOLO model
    model = YOLO("yolo26n.pt")
    
    # Perform object detection on an image
    results = model("https://ultralytics.com/images/bus.jpg")
    
    # Visualize the results
    for result in results:
        result.show()

Veja exemplos mais detalhados em nossa seção de [Modo de Previsão (Predict Mode)](/pt/modes/predict).

### Link to this sectionQuais são os diferentes modos disponíveis no YOLO?#

O Ultralytics YOLO oferece vários modos para atender a diferentes fluxos de trabalho de [aprendizado de máquina](https://www.ultralytics.com/glossary/machine-learning-ml). Eles incluem:

  * **[Train](/pt/modes/train)** : Treine um modelo usando conjuntos de dados personalizados.
  * **[Val](/pt/modes/val)** : Valide o desempenho do modelo em um conjunto de validação.
  * **[Predict](/pt/modes/predict)** : Faça previsões em novas imagens ou fluxos de vídeo.
  * **[Export](/pt/modes/export)** : Exporte modelos para vários formatos como ONNX e TensorRT.
  * **[Track](/pt/modes/track)** : Rastreamento de objetos em tempo real em fluxos de vídeo.
  * **[Benchmark](/pt/modes/benchmark)** : Avalie o desempenho do modelo em diferentes configurações.



Cada modo foi projetado para fornecer funcionalidades abrangentes para diferentes estágios do [desenvolvimento e implantação de modelos](https://docs.ultralytics.com/guides/model-deployment-options).

### Link to this sectionComo faço para treinar um modelo YOLO personalizado usando meu conjunto de dados?#

Para treinar um modelo YOLO personalizado, você precisa especificar seu conjunto de dados e outros [hiperparâmetros](https://www.ultralytics.com/glossary/hyperparameter-tuning). Aqui está um exemplo rápido:
    
    
    from ultralytics import YOLO
    
    # Load the YOLO model
    model = YOLO("yolo26n.yaml")
    
    # Train the model with custom dataset
    model.train(data="path/to/your/dataset.yaml", epochs=10)

Para obter mais detalhes sobre o treinamento e hiperlinks para exemplos de uso, visite nossa página de [Modo de Treinamento (Train Mode)](/pt/modes/train).

### Link to this sectionComo faço para exportar modelos YOLO para implantação?#

Exportar modelos YOLO em um formato adequado para implantação é direto com a função `export`. Por exemplo, você pode exportar um modelo para o formato ONNX:
    
    
    from ultralytics import YOLO
    
    # Load the YOLO model
    model = YOLO("yolo26n.pt")
    
    # Export the model to ONNX format
    model.export(format="onnx")

Para várias opções de exportação, consulte a documentação do [Modo de Exportação (Export Mode)](/pt/modes/export).

### Link to this sectionPosso validar meu modelo YOLO em diferentes conjuntos de dados?#

Sim, validar modelos YOLO em diferentes conjuntos de dados é possível. Após o treinamento, você pode usar o modo de validação para avaliar o desempenho:
    
    
    from ultralytics import YOLO
    
    # Load a YOLO model
    model = YOLO("yolo26n.yaml")
    
    # Train the model
    model.train(data="coco8.yaml", epochs=5)
    
    # Validate the model on a different dataset
    model.val(data="path/to/separate/data.yaml")

Verifique a página do [Modo de Validação (Val Mode)](/pt/modes/val) para exemplos detalhados e uso.

Colaboradores

[](https://github.com/ultralytics/ultralytics/blob/main/docs/en/usage/python.md)

[GLglenn-jocher20](https://github.com/glenn-jocher)[RIRizwanMunawar3](https://github.com/RizwanMunawar)[JKjk4e2](https://github.com/jk4e)[RAraimbekovm1](https://github.com/raimbekovm)[CDcdeil1](https://github.com/cdeil)[LEleonnil1](https://github.com/leonnil)[MAMatthewNoyce1](https://github.com/MatthewNoyce)[IVIvorZhu3311](https://github.com/IvorZhu331)[AYAyushExel1](https://github.com/AyushExel)

Criado 12 de nov. de 2023Atualizado semana passada

## Comentários

Nesta página

Explorar com IA
