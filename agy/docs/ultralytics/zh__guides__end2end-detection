---
source_url: https://docs.ultralytics.com/zh/guides/end2end-detection
---

[GitHub131.6k](https://github.com/ultralytics/ultralytics)

[](https://github.com/ultralytics/ultralytics/blob/main/docs/en/guides/end2end-detection.md)

# Link to this section了解 Ultralytics YOLO26 中的端到端检测#

## Link to this section简介#

If you're upgrading to [YOLO26](/zh/models/yolo26) from an earlier model like [YOLOv8](/zh/models/yolov8) or [YOLO11](/zh/models/yolo11), one of the biggest changes you'll notice is the removal of [Non-Maximum Suppression](https://www.ultralytics.com/glossary/non-maximum-suppression-nms) (NMS). Traditional YOLO models produce thousands of overlapping predictions that need a separate NMS post-processing step to filter down to final detections. This adds latency, complicates export graphs, and can behave inconsistently across different hardware platforms.

YOLO26 takes a different approach. It outputs final detections directly from the model — no external filtering required. This is known as **end-to-end[object detection](https://www.ultralytics.com/glossary/object-detection)**, and it's enabled by default in all YOLO26 models. The result is a simpler deployment pipeline, lower latency, and up to **43% faster inference on CPUs**.

本指南将带你了解具体变化、是否需要更新代码、哪些导出格式支持端到端推理，以及如何从旧版 YOLO 模型顺利迁移。

要深入了解此次架构转变背后的动机，请参阅 [Ultralytics 博客文章：为什么 YOLO26 移除了 NMS 以及它如何改变部署方式](https://www.ultralytics.com/blog/why-ultralytics-yolo26-removes-nms-and-how-that-changes-deployment)。

快速摘要

  * **正在使用 Ultralytics API 或 CLI？** 无需任何更改——只需将模型名称更换为 `yolo26n.pt` 即可。
  * **正在使用自定义推理代码（ONNX Runtime、TensorRT 等）？** 请更新你的后处理逻辑——检测输出现在为 `(N, 300, 6)`，格式为 `xyxy`，无需 NMS。其他任务会附加额外数据（掩码系数、关键点或角度）。
  * **正在导出？** 大多数格式原生支持端到端输出。然而，少数格式（NCNN、RKNN、PaddlePaddle、ExecuTorch、IMX、Edge TPU 和 QNN）由于不受支持的算子限制（例如 `torch.topk`），会自动回退到传统输出。Hailo HEF 工作流是从 ONNX 通过 Hailo 特定脚本编译的，因此请检查你模型的检测头和 NMS 配置。



## Link to this section端到端检测的工作原理#

YOLO26 在 [训练](/zh/modes/train) 期间使用**双头架构** 。两个头共享相同的主干和颈部，但以不同的方式产生输出：

检测头| 用途| 检测输出| 后处理  
---|---|---|---  
**一对一** (默认)| 端到端推理| `(N, 300, 6)`| 仅置信度阈值  
**一对多**|  传统 YOLO 输出| `(N, nc + 4, 8400)`| 需要 NMS  
  
上述形状适用于 [检测](/zh/tasks/detect)。其他任务通过在每个检测结果中添加额外数据来扩展一对一输出：

任务| 端到端输出| 额外数据  
---|---|---  
[检测](/zh/tasks/detect)| `(N, 300, 6)`| —  
[实例分割](/zh/tasks/segment)| `(N, 300, 6 + nm)` \+ proto `(N, nm, H, W)`| `nm` 掩码系数 (默认为 32)  
[姿态评估](/zh/tasks/pose)| `(N, 300, 57)`| 17 个关键点 × 3 (x, y, 可见性)  
[旋转框检测](/zh/tasks/obb)| `(N, 300, 7)`| 旋转角度  
  
在训练期间，两个头同时运行——一对多头提供更丰富的学习信号，而一对一头则学习产生干净、无重叠的预测。在 [推理](/zh/modes/predict) 和 [导出](/zh/modes/export) 期间，默认仅激活 **一对一头** ，每张图像最多产生 300 个检测结果，格式为 `[x1, y1, x2, y2, confidence, class_id]`。

当你调用 `model.fuse()` 时，它会合并 Conv + BatchNorm 层以实现更快的推理，并且在端到端模型上，还会移除一对多头，从而减小模型大小和 FLOPs。关于双头架构的更多详细信息，请参阅 [YOLO26 模型页面](/zh/models/yolo26)。

## Link to this section我需要更改代码吗？#

### Link to this section使用 Ultralytics Python API 或 CLI#

**无需任何更改。** 如果你使用标准的 [Ultralytics Python API](/zh/usage/python) 或 [CLI](/zh/usage/cli)，一切都会自动运行——[预测](/zh/modes/predict)、[验证](/zh/modes/val) 和 [导出](/zh/modes/export) 都能直接处理端到端模型。

使用 Ultralytics API 无需代码更改
    
    
    from ultralytics import YOLO
    
    # Load a YOLO26 model
    model = YOLO("yolo26n.pt")
    
    # Predict — no NMS step, no code changes
    results = model.predict("image.jpg")

### Link to this section使用自定义推理代码#

**是的，输出格式不同。** 如果你为 [YOLOv8](/zh/models/yolov8) 或 [YOLO11](/zh/models/yolo11) 编写了自定义后处理逻辑（例如，在使用 [ONNX Runtime](/zh/integrations/onnx) 或 [TensorRT](/zh/integrations/tensorrt) 运行推理时），你需要对其进行更新以处理新的输出形状：

| YOLOv8 / YOLO11| YOLO26 (端到端)  
---|---|---  
**检测输出**| `(N, nc + 4, 8400)`| `(N, 300, 6)`  
**框格式**| `xywh` (中心 x, 中心 y, 宽度, 高度)| `xyxy` (左上角 x, 左上角 y, 右下角 x, 右下角 y)  
**布局**|  每锚点的框坐标 + 分类得分| `[x1, y1, x2, y2, conf, class_id]`  
**需要 NMS**|  是| 否  
**后处理**|  NMS + 置信度过滤| 仅置信度过滤  
  
对于 [分割](/zh/tasks/segment)、[姿态评估](/zh/tasks/pose) 和 [旋转框检测](/zh/tasks/obb) 任务，YOLO26 会在每个检测结果中附加任务特定的数据——请参见上方的 输出形状表格。

其中 `N` 是 [批大小](https://www.ultralytics.com/glossary/batch-size)，`nc` 是类别数量（例如，[COCO](/zh/datasets/detect/coco) 为 80）。

使用端到端模型后，后处理变得简单得多——例如，在使用 [ONNX Runtime](/zh/integrations/onnx) 时：
    
    
    import onnxruntime as ort
    
    # Load and run the exported end-to-end model
    session = ort.InferenceSession("yolo26n.onnx")
    output = session.run(None, {session.get_inputs()[0].name: input_tensor})
    
    # End-to-end output: (batch, 300, 6) → [x1, y1, x2, y2, confidence, class_id]
    detections = output[0][0]  # first image in batch
    detections = detections[detections[:, 4] > conf_threshold]  # confidence filter — that's it!

### Link to this section切换到一对多头#

如果你需要传统的 YOLO 输出格式（例如，为了重复使用现有的基于 NMS 的后处理代码），你可以通过设置 `end2end=False` 随时切换到一对多头：

使用一对多头进行传统的基于 NMS 的输出
    
    
    from ultralytics import YOLO
    
    model = YOLO("yolo26n.pt")
    
    # Prediction with NMS (traditional behavior)
    results = model.predict("image.jpg", end2end=False)
    
    # Validation with NMS
    metrics = model.val(data="coco.yaml", end2end=False)
    
    # Export without end-to-end
    model.export(format="onnx", end2end=False)

## Link to this section导出格式兼容性#

大多数 [导出格式](/zh/modes/export#export-formats) 都开箱即用支持端到端推理，包括 [ONNX](/zh/integrations/onnx)、[TensorRT](/zh/integrations/tensorrt)、[CoreML](/zh/integrations/coreml)、[OpenVINO](/zh/integrations/openvino)、[TFLite](/zh/integrations/tflite)、[TF.js](/zh/integrations/tfjs) 和 [MNN](/zh/integrations/mnn)。

以下格式 **不支持** 端到端，会自动回退到一对多头：[NCNN](/zh/integrations/ncnn)、[RKNN](/zh/integrations/rockchip-rknn)、[PaddlePaddle](/zh/integrations/paddlepaddle)、[ExecuTorch](/zh/integrations/executorch)、[IMX](/zh/integrations/sony-imx500)、[Edge TPU](/zh/integrations/edge-tpu) 和 [Qualcomm QNN](/zh/integrations/qnn)。

当不支持端到端时会发生什么

当你导出到这些格式之一时，Ultralytics 会自动切换到一对多头并记录警告——无需手动干预。这意味着对于这些格式，**你需要在推理管道中包含 NMS** ，就像 [YOLOv8](/zh/models/yolov8) 或 [YOLO11](/zh/models/yolo11) 一样。

对于 [Hailo HEF](/zh/integrations/hailo)，编译步骤发生在 ONNX 导出之后的 `model.export(format=...)` 之外。请使用与你确切检测模型匹配的 Hailo DFC 日志、`.alls` 模型脚本和 NMS JSON；如果你的 Hailo 工具链不支持端到端的 YOLO26 图，请使用 `end2end=False` 导出 ONNX 模型并编译传统的检测头。

TensorRT + INT8

[TensorRT](/zh/integrations/tensorrt) 支持端到端，但在 JetPack 6 上的 TensorRT 10.3.0 中使用 `int8=True` 导出时，它会被 **自动禁用** 。

## Link to this section准确性和速度的权衡#

端到端检测在提供显著部署优势的同时，对 [准确性](https://www.ultralytics.com/glossary/accuracy) 的影响极小：

指标| 端到端 (默认)| 一对多 + NMS (`end2end=False`)  
---|---|---  
**CPU 推理速度**|  最快提升 **43%**|  基准  
**mAP 影响**|  ~0.5 mAP 较低| 匹配或超过 YOLO11  
**后处理**|  仅置信度过滤| 完整的 NMS 管道  
**部署复杂度**|  极低| 需要 NMS 实现  
  
对于大多数实际应用，~0.5 [mAP](https://www.ultralytics.com/glossary/mean-average-precision-map) 的差异可以忽略不计，尤其是在考虑到速度和简化带来的收益时。如果追求最大准确性是你的首要任务，你可以随时通过设置 `end2end=False` 回退到一对多头。

有关所有模型大小 (n, s, m, l, x) 的详细基准测试，请参阅 [YOLO26 性能指标](/zh/models/yolo26#performance-metrics)。

## Link to this section从 YOLOv8 或 YOLO11 迁移#

如果你正在将现有项目升级到 YOLO26，以下是确保平稳过渡的快速检查清单：

  * **Ultralytics API / CLI 用户：** 无需任何更改——只需将模型名称更新为 `yolo26n.pt`（或 `yolo26n-seg.pt`、`yolo26n-pose.pt`、`yolo26n-obb.pt`）
  * **自定义后处理代码：** 更新以处理新的输出形状——检测为 `(N, 300, 6)`，加上 [分割](/zh/tasks/segment)、[姿态评估](/zh/tasks/pose) 和 [旋转框检测](/zh/tasks/obb) 的任务特定数据。还要注意框格式从 `xywh` 到 `xyxy` 的变化。
  * **导出管道：** 请查看上述 格式兼容性 部分以了解你的目标格式。
  * **TensorRT + INT8：** 在 JetPack 6 上，TensorRT 10.3.0 在 `int8=True` 时会自动禁用端到端——请使用其他 TensorRT 版本以保留端到端。
  * **FP16 导出：** 如果你需要所有输出为 FP16，请使用 `end2end=False` 进行导出——请参阅 [为什么在导出时使用 half=True 和 end2end=True 时 output0 保持为 FP32](/zh/modes/export#why-is-output0-fp32-when-exporting-with-halftrue-and-end2endtrue)。
  * **iOS / CoreML：** 完全支持端到端。如果你需要 Xcode 预览支持，请将 `end2end=False` 与 `nms=True` 一起使用。
  * **边缘设备 (NCNN, RKNN)：** 这些格式会自动回退到一对多，因此请在你的设备端管道中包含 NMS。



## Link to this section常见问题解答#

### Link to this section我可以同时使用 end2end=True 和 nms=True 吗？#

No. These options are mutually exclusive. If you set `nms=True` on an end-to-end model during [export](/zh/modes/export), it will be automatically forced to `nms=False` with a warning. The end-to-end head already handles duplicate filtering internally, so external NMS is unnecessary.

然而，`end2end=False` 结合 `nms=True` 是一种有效的配置——它将传统的 NMS 植入导出图中。这对于 [CoreML](/zh/integrations/coreml) 导出很有用，因为它允许你直接使用带有检测模型的 Xcode 预览功能。

### Link to this section端到端模型中的 max_det 参数控制什么？#

`max_det` 参数（默认值：300）设置了一对一检测头每张图像可以输出的最大检测数量。你可以在推理或导出时进行调整：
    
    
    model.predict("image.jpg", max_det=100)  # fewer detections, slightly faster
    model.export(format="onnx", max_det=500)  # more detections for dense scenes

请注意，默认的 YOLO26 检查点是在 `max_det=300` 的条件下训练的。虽然你可以增加此值，但一对一检测头在训练过程中经过优化，最多只能产生 300 个清晰的检测结果，因此超过该限制的检测质量可能会降低。如果你每张图像需要超过 300 个检测结果，请考虑使用更高的 `max_det` 值进行重新训练。

### Link to this section我导出的 ONNX 模型输出 (1, 300, 6) —— 这是正确的吗？#

是的，这是预期的端到端检测输出格式：[batch size](https://www.ultralytics.com/glossary/batch-size) 为 1，最多 300 个检测，每个检测包含 6 个值 `[x1, y1, x2, y2, confidence, class_id]`。只需按置信度阈值进行过滤即可 —— 无需 NMS。

对于其他任务，输出形状会有所不同：

任务| 输出形状| 描述  
---|---|---  
检测| `(1, 300, 6)`| `[x1, y1, x2, y2, conf, class_id]`  
实例分割| `(1, 300, 38)` \+ `(1, 32, 160, 160)`| 6 个框值 + 32 个掩码系数，外加一个原型掩码张量  
姿态| `(1, 300, 57)`| 6 个框值 + 17 个关键点 × 3 (x, y, 可见度)  
OBB| `(1, 300, 7)`| 6 个框值 + 1 个旋转角度  
  
### Link to this section如何检查我导出的模型是否为端到端模型？#

你可以使用 Ultralytics Python API 或直接检查导出的 ONNX 模型元数据来查看：

检查模型是否为端到端
    
    
    from ultralytics import YOLO
    
    model = YOLO("yolo26n.onnx")
    model.predict(verbose=False)  # run predict to setup predictor first
    print(model.predictor.model.end2end)  # True if end-to-end is enabled

或者，检查输出形状 —— 端到端检测模型输出 `(1, 300, 6)`，而传统模型输出 `(1, nc + 4, 8400)`。关于其他任务的形状，请参阅 输出形状常见问题解答。

### Link to this section实例分割、姿态估计和 OBB 任务支持端到端吗？#

是的。YOLO26 检测风格的任务变体 —— [检测](/zh/tasks/detect)、[实例分割](/zh/tasks/segment)、[姿态估计](/zh/tasks/pose) 和 [旋转目标检测 (OBB)](/zh/tasks/obb) —— 默认支持端到端推理。这些任务也都提供 `end2end=False` 的回退选项。

每种任务都在基础检测输出之上扩展了特定任务的数据：

任务| 模型| 端到端输出  
---|---|---  
检测| `yolo26n.pt`| `(N, 300, 6)`  
实例分割| `yolo26n-seg.pt`| `(N, 300, 38)` \+ proto `(N, 32, 160, 160)`  
姿态| `yolo26n-pose.pt`| `(N, 300, 57)`  
OBB| `yolo26n-obb.pt`| `(N, 300, 7)`  
  
贡献者

[](https://github.com/ultralytics/ultralytics/blob/main/docs/en/guides/end2end-detection.md)

[GLglenn-jocher5](https://github.com/glenn-jocher)[AMambitious-octopus1](https://github.com/ambitious-octopus)[RAraimbekovm1](https://github.com/raimbekovm)

已创建 3个月前已更新 上周

## 评论

本页内容

使用 AI 探索
