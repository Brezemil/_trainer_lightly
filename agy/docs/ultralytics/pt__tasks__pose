---
source_url: https://docs.ultralytics.com/pt/tasks/pose
---

[GitHub131.6k](https://github.com/ultralytics/ultralytics)

[](https://github.com/ultralytics/ultralytics/blob/main/docs/en/tasks/pose.md)

# Link to this sectionEstimativa de pose#

![YOLO pose estimation with human body keypoint detection](https://cdn.jsdelivr.net/gh/ultralytics/assets@main/docs/pose-estimation-examples.avif)

A estimativa de pose é uma tarefa que envolve identificar a localização de pontos específicos em uma imagem, geralmente chamados de keypoints (pontos-chave). Os keypoints podem representar várias partes do objeto, como articulações, marcos ou outras características distintas. As localizações dos keypoints são geralmente representadas como um conjunto de coordenadas `[x, y]` em 2D, opcionalmente com um sinalizador de visibilidade `[x, y, visible]`.

A saída de um modelo de estimativa de pose é um conjunto de pontos que representam os keypoints em um objeto na imagem, geralmente junto com as pontuações de confiança para cada ponto. A estimativa de pose é uma boa escolha quando você precisa identificar partes específicas de um objeto em uma cena e sua localização em relação umas às outras.

  


  
**Watch:** How to Run Real-Time Pose Estimation with Ultralytics YOLO26 | Tracking & Keypoints Extraction 🕺 

Dica

Os modelos _pose_ do YOLO26 usam o sufixo `-pose`, por exemplo, `yolo26n-pose.pt`. Esses modelos são treinados no conjunto de dados [COCO keypoints](https://github.com/ultralytics/ultralytics/blob/main/ultralytics/cfg/datasets/coco-pose.yaml) e são adequados para uma variedade de tarefas de estimativa de pose.

No modelo de pose padrão do YOLO26, existem 17 keypoints, cada um representando uma parte diferente do corpo humano. Aqui está o mapeamento de cada índice para sua respectiva articulação corporal:

  0. Nariz
  1. Olho Esquerdo
  2. Olho Direito
  3. Orelha Esquerda
  4. Orelha Direita
  5. Ombro Esquerdo
  6. Ombro Direito
  7. Cotovelo Esquerdo
  8. Cotovelo Direito
  9. Pulso Esquerdo
  10. Pulso Direito
  11. Quadril Esquerdo
  12. Quadril Direito
  13. Joelho Esquerdo
  14. Joelho Direito
  15. Tornozelo Esquerdo
  16. Tornozelo Direito



## Link to this section[Modelos](https://github.com/ultralytics/ultralytics/tree/main/ultralytics/cfg/models/26)#

Os modelos de Pose pré-treinados do Ultralytics YOLO26 são mostrados aqui. Modelos de Detecção (Detect), Segmentação (Segment) e Pose são pré-treinados no conjunto de dados [COCO](https://github.com/ultralytics/ultralytics/blob/main/ultralytics/cfg/datasets/coco.yaml), modelos [Semânticos](/pt/tasks/semantic) são pré-treinados no [Cityscapes](/pt/datasets/semantic/cityscapes), e modelos de Classificação (Classify) são pré-treinados no conjunto de dados [ImageNet](https://github.com/ultralytics/ultralytics/blob/main/ultralytics/cfg/datasets/ImageNet.yaml).

Os [Modelos](https://github.com/ultralytics/ultralytics/tree/main/ultralytics/cfg/models) são baixados automaticamente da [versão](https://github.com/ultralytics/assets/releases) mais recente do Ultralytics no primeiro uso.

Modelo| tamanho  
(pixels)| mAPpose  
50-95(e2e)| mAPpose  
50(e2e)| Velocidade  
CPU ONNX  
(ms)| Velocidade  
T4 TensorRT10  
(ms)| params  
(M)| FLOPs  
(B)  
---|---|---|---|---|---|---|---  
[YOLO26n-pose](https://github.com/ultralytics/assets/releases/download/v8.4.0/yolo26n-pose.pt)| 640| 57.2| 83.3| 40.3 ± 0.5| 1.8 ± 0.0| 2.9| 7.5  
[YOLO26s-pose](https://github.com/ultralytics/assets/releases/download/v8.4.0/yolo26s-pose.pt)| 640| 63.0| 86.6| 85.3 ± 0.9| 2.7 ± 0.0| 10.4| 23.9  
[YOLO26m-pose](https://github.com/ultralytics/assets/releases/download/v8.4.0/yolo26m-pose.pt)| 640| 68.8| 89.6| 218.0 ± 1.5| 5.0 ± 0.1| 21.5| 73.1  
[YOLO26l-pose](https://github.com/ultralytics/assets/releases/download/v8.4.0/yolo26l-pose.pt)| 640| 70.4| 90.5| 275.4 ± 2.4| 6.5 ± 0.1| 25.9| 91.3  
[YOLO26x-pose](https://github.com/ultralytics/assets/releases/download/v8.4.0/yolo26x-pose.pt)| 640| 71.6| 91.6| 565.4 ± 3.0| 12.2 ± 0.2| 57.6| 201.7  
  
  * Os valores de **mAP val** são para modelo único em escala única no conjunto de dados [COCO Keypoints val2017](https://cocodataset.org/).   
Reproduza com `yolo val pose data=coco-pose.yaml device=0`
  * A **Velocidade** (Speed) é calculada com base na média das imagens de validação do COCO usando uma instância [Amazon EC2 P4d](https://aws.amazon.com/ec2/instance-types/p4/).   
Reproduza com `yolo val pose data=coco-pose.yaml batch=1 device=0|cpu`
  * Os valores de **Parâmetros** (Params) e **FLOPs** são para o modelo fundido após `model.fuse()`, que mescla camadas Conv e BatchNorm e, para modelos end-to-end, remove a cabeça de detecção auxiliar one-to-many. Checkpoints pré-treinados mantêm a arquitetura de treinamento completa e podem apresentar contagens maiores.



## Link to this sectionTreinar#

Treine um modelo YOLO26-pose no conjunto de dados COCO8-pose. O [conjunto de dados COCO8-pose](https://docs.ultralytics.com/datasets/pose/coco8-pose) é um pequeno conjunto de dados de amostra perfeito para testar e depurar seus modelos de estimativa de pose.

Exemplo
    
    
    from ultralytics import YOLO
    
    # Load a model
    model = YOLO("yolo26n-pose.yaml")  # build a new model from YAML
    model = YOLO("yolo26n-pose.pt")  # load a pretrained model (recommended for training)
    model = YOLO("yolo26n-pose.yaml").load("yolo26n-pose.pt")  # build from YAML and transfer weights
    
    # Train the model
    results = model.train(data="coco8-pose.yaml", epochs=100, imgsz=640)

Veja detalhes completos do modo `train` na página [Treinar](/pt/modes/train). Modelos de pose também podem ser treinados em GPUs na nuvem através da [Plataforma Ultralytics](https://platform.ultralytics.com).

### Link to this sectionFormato do conjunto de dados#

O formato do conjunto de dados de pose YOLO pode ser encontrado em detalhes no [Guia de Conjuntos de Dados](/pt/datasets/pose). Para converter seu conjunto de dados existente de outros formatos (como [COCO](https://docs.ultralytics.com/datasets/pose/coco) etc.) para o formato YOLO, utilize a ferramenta [JSON2YOLO](https://github.com/ultralytics/JSON2YOLO) da Ultralytics. A [Plataforma Ultralytics](https://platform.ultralytics.com) também suporta anotação de pose com modelos de esqueleto integrados para pessoas, mãos, rostos e layouts de keypoints personalizados.

Para tarefas personalizadas de estimativa de pose, você também pode explorar conjuntos de dados especializados como [Tiger-Pose](https://docs.ultralytics.com/datasets/pose/tiger-pose) para estimativa de pose animal, [Hand Keypoints](https://docs.ultralytics.com/datasets/pose/hand-keypoints) para rastreamento de mãos, ou [Dog-Pose](https://docs.ultralytics.com/datasets/pose/dog-pose) para análise de pose canina.

## Link to this sectionValidar#

Valide a [precisão](https://www.ultralytics.com/glossary/accuracy) do modelo YOLO26n-pose treinado no conjunto de dados COCO8-pose. Nenhum argumento é necessário, pois o `model` retém seus dados (`data`) de treinamento e argumentos como atributos do modelo.

Exemplo
    
    
    from ultralytics import YOLO
    
    # Load a model
    model = YOLO("yolo26n-pose.pt")  # load an official model
    model = YOLO("path/to/best.pt")  # load a custom model
    
    # Validate the model
    metrics = model.val()  # no arguments needed, dataset and settings remembered
    metrics.box.map  # map50-95
    metrics.box.map50  # map50
    metrics.box.map75  # map75
    metrics.box.maps  # a list containing mAP50-95 for each category
    metrics.box.image_metrics  # per-image metrics dictionary for box with precision, recall, F1, TP, FP, and FN
    metrics.pose.map  # map50-95(P)
    metrics.pose.map50  # map50(P)
    metrics.pose.map75  # map75(P)
    metrics.pose.maps  # a list containing mAP50-95(P) for each category
    metrics.pose.image_metrics  # per-image metrics dictionary for pose with precision, recall, F1, TP, FP, and FN

## Link to this sectionPrever#

Use um modelo YOLO26n-pose treinado para realizar previsões em imagens. O [modo predict](https://docs.ultralytics.com/modes/predict) permite que você execute inferência em imagens, vídeos ou fluxos em tempo real.

Exemplo
    
    
    from ultralytics import YOLO
    
    # Load a model
    model = YOLO("yolo26n-pose.pt")  # load an official model
    model = YOLO("path/to/best.pt")  # load a custom model
    
    # Predict with the model
    results = model("https://ultralytics.com/images/bus.jpg")  # predict on an image
    
    # Access the results
    for result in results:
        xy = result.keypoints.xy  # x and y coordinates
        xyn = result.keypoints.xyn  # normalized
        kpts = result.keypoints.data  # x, y, visibility (if available)

Veja os detalhes completos do modo `predict` na página [Previsão](/pt/modes/predict).

### Link to this sectionSaída de Resultados#

A estimativa de pose retorna um objeto `Results` por imagem. Os campos de previsão primários são `result.keypoints` para as coordenadas da pose e `result.boxes` para as instâncias detectadas às quais esses keypoints pertencem.

Atributo| Tipo| Forma| Descrição  
---|---|---|---  
`result.keypoints`| `Keypoints`| `(N)`| Pontos-chave.  
`result.keypoints.data`| `torch.float32`| `(N,K,2/3)`| `x,y` mais visibilidade/confiança opcional.  
`result.keypoints.xy`| `torch.float32`| `(N,K,2)`| Pontos-chave em pixels.  
`result.keypoints.xyn`| `torch.float32`| `(N,K,2)`| Pontos-chave normalizados.  
`result.boxes`| `Boxes`| `(N)`| Caixas de instância.  
  
Para campos de `Results` específicos de cada tarefa em todas as tarefas, veja a seção [Resultados de Previsão por Tarefa](/pt/modes/predict#results-by-task).

## Link to this sectionExportar#

Exporte um modelo YOLO26n Pose para um formato diferente como ONNX, CoreML, etc. Isso permite que você implante seu modelo em várias plataformas e dispositivos para [inferência em tempo real](https://www.ultralytics.com/glossary/real-time-inference).

Exemplo
    
    
    from ultralytics import YOLO
    
    # Load a model
    model = YOLO("yolo26n-pose.pt")  # load an official model
    model = YOLO("path/to/best.pt")  # load a custom-trained model
    
    # Export the model
    model.export(format="onnx")

Os formatos de exportação disponíveis para o YOLO26-pose estão na tabela abaixo. Você pode exportar para qualquer formato usando o argumento `format`, por exemplo, `format='onnx'` ou `format='engine'`. Você pode prever ou validar diretamente em modelos exportados, por exemplo, `yolo predict model=yolo26n-pose.onnx`. Exemplos de uso são mostrados para seu modelo após a conclusão da exportação.

Formato| Argumento `format`| Modelo| Metadados| Argumentos  
---|---|---|---|---  
[PyTorch](https://pytorch.org/)| -| `yolo26n-pose.pt`| ✅| -  
[TorchScript](/pt/integrations/torchscript)| `torchscript`| `yolo26n-pose.torchscript`| ✅| `imgsz`, `half`, `dynamic`, `optimize`, `nms`, `batch`, `device`  
[ONNX](/pt/integrations/onnx)| `onnx`| `yolo26n-pose.onnx`| ✅| `imgsz`, `half`, `int8`, `dynamic`, `simplify`, `opset`, `nms`, `batch`, `data`, `fraction`, `device`  
[OpenVINO](/pt/integrations/openvino)| `openvino`| `yolo26n-pose_openvino_model/`| ✅| `imgsz`, `half`, `dynamic`, `int8`, `nms`, `batch`, `data`, `fraction`, `device`  
[TensorRT](/pt/integrations/tensorrt)| `engine`| `yolo26n-pose.engine`| ✅| `imgsz`, `half`, `dynamic`, `simplify`, `workspace`, `int8`, `nms`, `batch`, `data`, `fraction`, `device`  
[CoreML](/pt/integrations/coreml)| `coreml`| `yolo26n-pose.mlpackage`| ✅| `imgsz`, `dynamic`, `half`, `int8`, `nms`, `batch`, `device`  
[TF SavedModel](/pt/integrations/tf-savedmodel)| `saved_model`| `yolo26n-pose_saved_model/`| ✅| `imgsz`, `keras`, `int8`, `nms`, `batch`, `data`, `fraction`, `device`  
[TF GraphDef](/pt/integrations/tf-graphdef)| `pb`| `yolo26n-pose.pb`| ❌| `imgsz`, `batch`, `device`  
[TF Lite](/pt/integrations/tflite)| `tflite`| `yolo26n-pose.tflite`| ✅| `imgsz`, `half`, `int8`, `nms`, `batch`, `data`, `fraction`, `device`  
[TF Edge TPU](/pt/integrations/edge-tpu)| `edgetpu`| `yolo26n-pose_edgetpu.tflite`| ✅| `imgsz`, `int8`, `data`, `fraction`, `device`  
[TF.js](/pt/integrations/tfjs)| `tfjs`| `yolo26n-pose_web_model/`| ✅| `imgsz`, `half`, `int8`, `nms`, `batch`, `data`, `fraction`, `device`  
[PaddlePaddle](/pt/integrations/paddlepaddle)| `paddle`| `yolo26n-pose_paddle_model/`| ✅| `imgsz`, `batch`, `device`  
[MNN](/pt/integrations/mnn)| `mnn`| `yolo26n-pose.mnn`| ✅| `imgsz`, `batch`, `int8`, `half`, `device`  
[NCNN](/pt/integrations/ncnn)| `ncnn`| `yolo26n-pose_ncnn_model/`| ✅| `imgsz`, `half`, `batch`, `device`  
[IMX500](/pt/integrations/sony-imx500)| `imx`| `yolo26n-pose_imx_model/`| ✅| `imgsz`, `int8`, `data`, `fraction`, `nms`, `device`  
[RKNN](/pt/integrations/rockchip-rknn)| `rknn`| `yolo26n-pose_rknn_model/`| ✅| `imgsz`, `batch`, `name`, `half`, `int8`, `data`, `fraction`, `device`  
[ExecuTorch](/pt/integrations/executorch)| `executorch`| `yolo26n-pose_executorch_model/`| ✅| `imgsz`, `batch`, `device`  
[Axelera](/pt/integrations/axelera)| `axelera`| `yolo26n-pose_axelera_model/`| ✅| `imgsz`, `batch`, `int8`, `data`, `fraction`, `device`  
[DEEPX](/pt/integrations/deepx)| `deepx`| `yolo26n-pose_deepx_model/`| ✅| `imgsz`, `int8`, `data`, `optimize`, `device`  
[Qualcomm QNN](/pt/integrations/qnn)| `qnn`| `yolo26n-pose_qnn.onnx`| ✅| `imgsz`, `batch`, `name`, `int8`, `data`, `fraction`, `device`  
  
Veja detalhes completos de `export` na página [Export](/pt/modes/export).

## Link to this sectionFAQ#

### Link to this sectionO que é Estimativa de Pose com Ultralytics YOLO26 e como funciona?#

A estimativa de pose com o Ultralytics YOLO26 envolve a identificação de pontos específicos, conhecidos como keypoints, em uma imagem. Esses keypoints normalmente representam articulações ou outras características importantes do objeto. A saída inclui as coordenadas `[x, y]` e pontuações de confiança para cada ponto. Os modelos YOLO26-pose são projetados especificamente para esta tarefa e usam o sufixo `-pose`, como `yolo26n-pose.pt`. Esses modelos são pré-treinados em conjuntos de dados como [COCO keypoints](https://github.com/ultralytics/ultralytics/blob/main/ultralytics/cfg/datasets/coco-pose.yaml) e podem ser usados para várias tarefas de estimativa de pose. Para mais informações, visite a Página de Estimativa de Pose.

### Link to this sectionComo posso treinar um modelo YOLO26-pose em um conjunto de dados personalizado?#

Treinar um modelo YOLO26-pose em um conjunto de dados personalizado envolve carregar um modelo, seja um novo modelo definido por um arquivo YAML ou um modelo pré-treinado. Você pode então iniciar o processo de treinamento usando seu conjunto de dados e parâmetros especificados.
    
    
    from ultralytics import YOLO
    
    # Load a model
    model = YOLO("yolo26n-pose.yaml")  # build a new model from YAML
    model = YOLO("yolo26n-pose.pt")  # load a pretrained model (recommended for training)
    
    # Train the model
    results = model.train(data="your-dataset.yaml", epochs=100, imgsz=640)

Para detalhes abrangentes sobre treinamento, consulte a Seção de Treinamento. Você também pode usar a [Plataforma Ultralytics](https://platform.ultralytics.com) para uma abordagem sem código para treinar modelos personalizados de estimativa de pose.

### Link to this sectionComo valido um modelo YOLO26-pose treinado?#

A validação de um modelo YOLO26-pose envolve avaliar sua precisão usando os mesmos parâmetros de conjunto de dados retidos durante o treinamento. Aqui está um exemplo:
    
    
    from ultralytics import YOLO
    
    # Load a model
    model = YOLO("yolo26n-pose.pt")  # load an official model
    model = YOLO("path/to/best.pt")  # load a custom model
    
    # Validate the model
    metrics = model.val()  # no arguments needed, dataset and settings remembered

Para mais informações, visite a Seção de Validação.

### Link to this sectionPosso exportar um modelo YOLO26-pose para outros formatos, e como?#

Sim, você pode exportar um modelo YOLO26-pose para vários formatos como ONNX, CoreML, TensorRT, entre outros. Isso pode ser feito usando Python ou a Interface de Linha de Comando (CLI).
    
    
    from ultralytics import YOLO
    
    # Load a model
    model = YOLO("yolo26n-pose.pt")  # load an official model
    model = YOLO("path/to/best.pt")  # load a custom-trained model
    
    # Export the model
    model.export(format="onnx")

Consulte a Seção de Exportação para mais detalhes. Modelos exportados podem ser implantados em dispositivos de borda para [aplicações em tempo real](https://www.ultralytics.com/blog/real-time-inferences-in-vision-ai-solutions-are-making-an-impact) como rastreamento de fitness, análise esportiva ou [robótica](https://www.ultralytics.com/blog/from-algorithms-to-automation-ais-role-in-robotics).

### Link to this sectionQuais são os modelos YOLO26-pose da Ultralytics disponíveis e suas métricas de desempenho?#

O Ultralytics YOLO26 oferece vários modelos de pose pré-treinados, como YOLO26n-pose, YOLO26s-pose, YOLO26m-pose, entre outros. Esses modelos diferem em tamanho, precisão (mAP) e velocidade. Por exemplo, o modelo YOLO26n-pose atinge um mAPpose50-95 de 57.2 e um mAPpose50 de 83.3. Para uma lista completa e detalhes de desempenho, visite a Seção de Modelos.

Colaboradores

[](https://github.com/ultralytics/ultralytics/blob/main/docs/en/tasks/pose.md)

[GLglenn-jocher33](https://github.com/glenn-jocher)[RIRizwanMunawar6](https://github.com/RizwanMunawar)[BUBurhan-Q4](https://github.com/Burhan-Q)[AMambitious-octopus3](https://github.com/ambitious-octopus)[RAraimbekovm2](https://github.com/raimbekovm)[LALaughing-q1](https://github.com/Laughing-q)[PDpderrenger1](https://github.com/pderrenger)[Y-Y-T-G1](https://github.com/Y-T-G)[JKjk4e1](https://github.com/jk4e)[MAMatthewNoyce1](https://github.com/MatthewNoyce)JFJF Chen1

Criado 12 de nov. de 2023Atualizado semana passada

## Comentários

Nesta página

Explorar com IA
