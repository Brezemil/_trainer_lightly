---
source_url: https://docs.ultralytics.com/zh/usage/python
---

[GitHub131.6k](https://github.com/ultralytics/ultralytics)

[](https://github.com/ultralytics/ultralytics/blob/main/docs/en/usage/python.md)

# Link to this sectionPython 用法#

欢迎来到 Ultralytics YOLO Python 用法文档！本指南旨在帮助你将 Ultralytics YOLO 无缝集成到你的 Python 项目中，用于 [目标检测](https://www.ultralytics.com/glossary/object-detection)、[实例分割](https://docs.ultralytics.com/tasks/segment)、[语义分割](https://docs.ultralytics.com/tasks/semantic) 和 [分类](https://docs.ultralytics.com/tasks/classify)。在此，你将学习如何加载和使用预训练模型、训练新模型以及对图像进行预测。简单易用的 Python 接口是希望将 YOLO 融入其 Python 项目的开发者的宝贵资源，让你能够快速实现先进的目标检测功能。让我们开始吧！

  


  
**Watch:** Mastering Ultralytics YOLO: Python 

例如，用户只需几行代码即可加载模型、进行训练、在验证集上评估其性能，甚至将其 [导出为 ONNX 格式](/zh/modes/export)。

Python
    
    
    from ultralytics import YOLO
    
    # Create a new YOLO model from scratch
    model = YOLO("yolo26n.yaml")
    
    # Load a pretrained YOLO model (recommended for training)
    model = YOLO("yolo26n.pt")
    
    # Train the model using the 'coco8.yaml' dataset for 3 epochs
    results = model.train(data="coco8.yaml", epochs=3)
    
    # Evaluate the model's performance on the validation set
    results = model.val()
    
    # Perform object detection on an image using the model
    results = model("https://ultralytics.com/images/bus.jpg")
    
    # Export the model to ONNX format
    success = model.export(format="onnx")

## Link to this section训练#

[训练模式 (Train mode)](/zh/modes/train) 用于在自定义数据集上训练 YOLO 模型。在此模式下，模型会使用指定的数据集和超参数进行训练。训练过程包括优化模型的参数，以便其能够准确预测图像中对象的类别和位置。

训练
    
    
    from ultralytics import YOLO
    
    model = YOLO("yolo26n.pt")  # pass any model type
    results = model.train(epochs=5)

[训练示例](/zh/modes/train)

## Link to this section验证#

[验证模式 (Val mode)](/zh/modes/val) 用于在 YOLO 模型训练完成后对其进行验证。在此模式下，模型会在验证集上进行评估，以衡量其 [准确率](https://www.ultralytics.com/glossary/accuracy) 和泛化性能。该模式可用于调整模型的超参数以提高其性能。

验证
    
    
    from ultralytics import YOLO
    
    # Load a YOLO model
    model = YOLO("yolo26n.yaml")
    
    # Train the model
    model.train(data="coco8.yaml", epochs=5)
    
    # Validate on training data
    model.val()

[验证示例](/zh/modes/val)

## Link to this section预测#

[预测模式 (Predict mode)](/zh/modes/predict) 用于使用训练好的 YOLO 模型对新图像或视频进行预测。在此模式下，模型从检查点文件加载，用户可以提供图像或视频来执行推理。模型会预测输入图像或视频中对象的类别和位置。

预测
    
    
    import cv2
    from PIL import Image
    
    from ultralytics import YOLO
    
    model = YOLO("model.pt")
    # accepts all formats - image/dir/Path/URL/video/PIL/ndarray. 0 for webcam
    results = model.predict(source="0")
    results = model.predict(source="folder", show=True)  # Display preds. Accepts all YOLO predict arguments
    
    # from PIL
    im1 = Image.open("bus.jpg")
    results = model.predict(source=im1, save=True)  # save plotted images
    
    # from ndarray
    im2 = cv2.imread("bus.jpg")
    results = model.predict(source=im2, save=True, save_txt=True)  # save predictions as labels
    
    # from list of PIL/ndarray
    results = model.predict(source=[im1, im2])

[预测示例](/zh/modes/predict)

## Link to this section导出#

[导出模式 (Export mode)](/zh/modes/export) 用于将 YOLO 模型导出为可用于部署的格式。在此模式下，模型会被转换为其他软件应用程序或硬件设备可以使用的格式。该模式在将模型部署到生产环境时非常有用。

导出

将官方 YOLO 模型导出为具有动态 batch-size 和 image-size 的 [ONNX](https://www.ultralytics.com/glossary/onnx-open-neural-network-exchange) 格式。
    
    
    from ultralytics import YOLO
    
    model = YOLO("yolo26n.pt")
    model.export(format="onnx", dynamic=True)

[导出示例](/zh/modes/export)

## Link to this section追踪#

[追踪模式 (Track mode)](/zh/modes/track) 用于使用 YOLO 模型进行实时对象追踪。在此模式下，模型从检查点文件加载，用户可以提供实时视频流来执行实时对象追踪。该模式对于监控系统或 [自动驾驶汽车](https://www.ultralytics.com/solutions/ai-in-automotive) 等应用非常有用。

追踪
    
    
    from ultralytics import YOLO
    
    # Load a model
    model = YOLO("yolo26n.pt")  # load an official detection model
    model = YOLO("yolo26n-seg.pt")  # load an official segmentation model
    model = YOLO("path/to/best.pt")  # load a custom model
    
    # Track with the model
    results = model.track(source="https://youtu.be/LNwODJXcvt4", show=True)
    results = model.track(source="https://youtu.be/LNwODJXcvt4", show=True, tracker="bytetrack.yaml")

[追踪示例](/zh/modes/track)

## Link to this section基准测试#

[基准测试模式 (Benchmark mode)](/zh/modes/benchmark) 用于分析各种导出格式的 YOLO 速度和准确率。基准测试提供了有关导出格式的大小、其 `mAP50-95` 指标（用于目标检测和分割）或 `accuracy_top1` 指标（用于分类），以及在 ONNX、[OpenVINO](https://docs.ultralytics.com/integrations/openvino)、TensorRT 等各种导出格式下每张图像的推理时间（毫秒）的信息。这些信息可以帮助用户根据其对速度和准确率的要求，为特定用例选择最佳的导出格式。

基准测试

对官方 YOLO 模型进行全导出格式基准测试。
    
    
    from ultralytics.utils.benchmarks import benchmark
    
    # Benchmark
    benchmark(model="yolo26n.pt", data="coco8.yaml", imgsz=640, half=False, device=0)

[基准测试示例](/zh/modes/benchmark)

## Link to this section使用训练器 (Trainers)#

`YOLO` 模型类充当了 Trainer 类的高级封装。每个 YOLO 任务都有自己的训练器，它们都继承自 `BaseTrainer`。这种架构允许在你的 [机器学习工作流](https://docs.ultralytics.com/guides/model-training-tips) 中实现更大的灵活性和定制化。

检测训练器示例
    
    
    from ultralytics.models.yolo.detect import DetectionPredictor, DetectionTrainer, DetectionValidator
    
    # trainer
    trainer = DetectionTrainer(overrides={})
    trainer.train()
    trained_model = trainer.best
    
    # Validator
    val = DetectionValidator(args=...)
    val(model=trained_model)
    
    # predictor
    pred = DetectionPredictor(overrides={})
    pred(source=SOURCE, model=trained_model)
    
    # resume from last weight
    overrides["resume"] = trainer.last
    trainer = DetectionTrainer(overrides=overrides)

你可以轻松定制训练器以支持自定义任务，或探索研发想法。Ultralytics YOLO 的模块化设计允许你根据特定需求调整框架，无论你是从事新颖的 [计算机视觉](https://www.ultralytics.com/glossary/computer-vision-cv) 任务，还是为了获得更好的性能而微调现有模型。

[定制化教程](/zh/usage/engine)

## Link to this section常见问题解答#

### Link to this section如何将 YOLO 集成到我的 Python 项目中进行目标检测？#

将 Ultralytics YOLO 集成到你的 Python 项目中非常简单。你可以加载预训练模型，或从零开始训练一个新模型。以下是入门步骤：
    
    
    from ultralytics import YOLO
    
    # Load a pretrained YOLO model
    model = YOLO("yolo26n.pt")
    
    # Perform object detection on an image
    results = model("https://ultralytics.com/images/bus.jpg")
    
    # Visualize the results
    for result in results:
        result.show()

在我们的 [预测模式](/zh/modes/predict) 部分查看更详细的示例。

### Link to this sectionYOLO 中有哪些不同的模式？#

Ultralytics YOLO 提供了各种模式来满足不同的 [机器学习](https://www.ultralytics.com/glossary/machine-learning-ml) 工作流需求。这些包括：

  * **[训练 (Train)](/zh/modes/train)** ：使用自定义数据集训练模型。
  * **[验证 (Val)](/zh/modes/val)** ：在验证集上验证模型性能。
  * **[预测 (Predict)](/zh/modes/predict)** ：对新图像或视频流进行预测。
  * **[导出 (Export)](/zh/modes/export)** ：将模型导出为 ONNX 和 TensorRT 等各种格式。
  * **[追踪 (Track)](/zh/modes/track)** ：视频流中的实时对象追踪。
  * **[基准测试 (Benchmark)](/zh/modes/benchmark)** ：针对不同配置对模型性能进行基准测试。



每种模式都旨在为 [模型开发和部署](https://docs.ultralytics.com/guides/model-deployment-options) 的不同阶段提供全面的功能。

### Link to this section如何使用我的数据集训练自定义 YOLO 模型？#

要训练自定义 YOLO 模型，你需要指定你的数据集和其他 [超参数](https://www.ultralytics.com/glossary/hyperparameter-tuning)。这是一个快速示例：
    
    
    from ultralytics import YOLO
    
    # Load the YOLO model
    model = YOLO("yolo26n.yaml")
    
    # Train the model with custom dataset
    model.train(data="path/to/your/dataset.yaml", epochs=10)

有关训练的更多详细信息和示例用法的超链接，请访问我们的 [训练模式](/zh/modes/train) 页面。

### Link to this section如何导出 YOLO 模型以便部署？#

使用 `export` 函数可以轻松地将 YOLO 模型导出为适合部署的格式。例如，你可以将模型导出为 ONNX 格式：
    
    
    from ultralytics import YOLO
    
    # Load the YOLO model
    model = YOLO("yolo26n.pt")
    
    # Export the model to ONNX format
    model.export(format="onnx")

有关各种导出选项，请参阅 [导出模式](/zh/modes/export) 文档。

### Link to this section我可以在不同的数据集上验证我的 YOLO 模型吗？#

可以，在不同数据集上验证 YOLO 模型是可行的。训练完成后，你可以使用验证模式来评估性能：
    
    
    from ultralytics import YOLO
    
    # Load a YOLO model
    model = YOLO("yolo26n.yaml")
    
    # Train the model
    model.train(data="coco8.yaml", epochs=5)
    
    # Validate the model on a different dataset
    model.val(data="path/to/separate/data.yaml")

查看 [验证模式](/zh/modes/val) 页面以获取详细示例和用法。

贡献者

[](https://github.com/ultralytics/ultralytics/blob/main/docs/en/usage/python.md)

[GLglenn-jocher20](https://github.com/glenn-jocher)[RIRizwanMunawar3](https://github.com/RizwanMunawar)[JKjk4e2](https://github.com/jk4e)[RAraimbekovm1](https://github.com/raimbekovm)[CDcdeil1](https://github.com/cdeil)[LEleonnil1](https://github.com/leonnil)[MAMatthewNoyce1](https://github.com/MatthewNoyce)[IVIvorZhu3311](https://github.com/IvorZhu331)[AYAyushExel1](https://github.com/AyushExel)

已创建 2023年11月12日已更新 上周

## 评论

本页内容

使用 AI 探索
