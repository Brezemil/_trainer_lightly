---
source_url: https://docs.ultralytics.com/zh/guides/yolo-common-issues
---

[GitHub131.6k](https://github.com/ultralytics/ultralytics)

[](https://github.com/ultralytics/ultralytics/blob/main/docs/en/guides/yolo-common-issues.md)

# Link to this section排查常见 YOLO 问题#

![YOLO common training and deployment issues](https://cdn.jsdelivr.net/gh/ultralytics/assets@main/docs/yolo-common-issues.avif)

## Link to this section简介#

本指南旨在为你提供全面的帮助，以排查在使用 YOLO26 进行 Ultralytics 项目开发时遇到的常见问题。通过正确的指引，你可以轻松解决这些问题，确保你的项目顺利推进，避免不必要的延迟。

  


  
**Watch:** Ultralytics YOLO26 Common Issues | Installation Errors, Model Training Issues 

## Link to this section常见问题#

### Link to this section安装错误#

安装错误可能由多种原因引起，例如版本不兼容、缺失依赖项或环境配置不正确。首先，请确保你已完成以下步骤：

  * 你正在按照建议使用 Python 3.8 或更高版本。
  * 确保你已安装正确版本的 [PyTorch](https://www.ultralytics.com/glossary/pytorch)（1.8 或更高版本）。
  * 考虑使用虚拟环境以避免冲突。
  * 请按照 [官方安装指南](/zh/quickstart) 一步步操作。



此外，以下是一些用户常见的安装问题及其对应的解决方案：

  * 导入错误或依赖项问题 - 如果你在导入 YOLO26 时遇到错误，或者遇到与依赖项相关的问题，请考虑以下排查步骤：

    * **全新安装** ：有时，通过全新安装可以解决意想不到的问题。尤其是在使用 Ultralytics 这类库时，更新可能会引入文件树结构或功能上的变化。

    * **定期更新** ：确保你使用的是该库的最新版本。旧版本可能与最近的更新不兼容，从而导致潜在的冲突或问题。

    * **检查依赖项** ：验证所有必需的依赖项是否已正确安装且版本兼容。

    * **查看更改** ：如果你最初克隆或安装的是旧版本，请注意重大更新可能会影响库的结构或功能。请务必查阅官方文档或更新日志以了解重大变更。

    * 请记住，保持你的库和依赖项处于最新状态对于获得流畅且无错误的体验至关重要。

  * 在 GPU 上运行 YOLO26 - 如果你在 GPU 上运行 YOLO26 时遇到困难，请考虑以下排查步骤：

    * **验证 CUDA 兼容性与安装** ：确保你的 GPU 支持 CUDA 且已正确安装 CUDA。使用 `nvidia-smi` 命令检查你的 NVIDIA GPU 状态和 CUDA 版本。

    * **检查 PyTorch 与 CUDA 集成** ：通过在 Python 终端运行 `import torch; print(torch.cuda.is_available())` 来确保 PyTorch 可以利用 CUDA。如果返回 'True'，则说明 PyTorch 已配置为使用 CUDA。

    * **检查 GPU 兼容性** ：自 cuDNN 9.11.0 起，已放弃对 Turing 架构之前以及计算能力 (SM) < 7.5 的 GPU 的支持。因此，如果你使用的是较旧的 GPU（如 1080Ti），你可能需要使用针对旧版 CUDA/cuDNN 构建的 PyTorch 版本。你可以通过运行 `import torch; cap = torch.cuda.get_device_capability(0) if torch.cuda.is_available() else (0, 0); cudnn = torch.backends.cudnn.version() or 0; ok = "not compatible" if cudnn >= 91100 and (cap[0] < 7 or (cap[0] == 7 and cap[1] < 5)) else "should be ok"; print(f"Compute capability: SM {cap[0]}.{cap[1]}, cuDNN: {cudnn} => {ok}")` 来进行检查。

    * **环境激活** ：确保你处于已安装所有必要软件包的正确环境中。

    * **更新你的软件包** ：过期的软件包可能与你的 GPU 不兼容。请保持它们更新。

    * **程序配置** ：检查程序或代码是否指定了 GPU 使用情况。在 YOLO26 中，这可能位于设置或配置中。




### Link to this section模型训练问题#

本节将解决训练过程中遇到的常见问题及其相应的解释和解决方案。

#### Link to this section验证配置设置#

**Issue** : You are unsure whether the configuration settings in the `.yaml` file are being applied correctly during model training.

**Solution** : The configuration settings in the `.yaml` file should be applied when using the `model.train()` function. To ensure that these settings are correctly applied, follow these steps:

  * Confirm that the path to your `.yaml` configuration file is correct.

  * Make sure you pass the path to your `.yaml` file as the `data` argument when calling `model.train()`, as shown below:
        
        model.train(data="/path/to/your/data.yaml", batch=4)




#### Link to this section使用多个 GPU 加速训练#

**问题** ：单 GPU 训练速度缓慢，你希望使用多个 GPU 加快这一过程。

**解决方案** ：增加 [batch size](https://www.ultralytics.com/glossary/batch-size) 可以加速训练，但必须考虑 GPU 内存容量。要使用多个 GPU 加速训练，请遵循以下步骤：

  * 确保你有多个可用的 GPU。

  * 将 `device` 参数设置为 GPU 索引列表，例如 `device=[0,1,2,3]`。

  * 相应地增加 batch size，以充分利用多个 GPU，同时不超过内存限制。

  * 修改你的训练命令以利用多个 GPU：
        
        # Adjust the batch size and other settings as needed to optimize training speed
        model.train(data="/path/to/your/data.yaml", batch=32, device=[0, 1, 2, 3])




#### Link to this section持续监控参数#

**问题** ：除了损失函数（loss）之外，你想知道训练期间还应该持续监控哪些参数。

**解决方案** ：虽然损失函数是一个关键的监控指标，但跟踪其他指标以进行模型性能优化也至关重要。训练期间需要监控的一些关键指标包括：

  * 精确率
  * 召回率
  * [Mean Average Precision](https://www.ultralytics.com/glossary/mean-average-precision-map) (mAP)



你可以从训练日志中获取这些指标，或者使用 TensorBoard 或 wandb 等工具进行可视化。基于这些指标实施提前停止（early stopping）策略可以帮助你获得更好的结果。

#### Link to this section跟踪训练进度的工具#

**问题** ：你正在寻找关于跟踪训练进度的工具推荐。

**解决方案** ：要跟踪并可视化训练进度，你可以考虑使用以下工具：

  * [TensorBoard](https://www.tensorflow.org/tensorboard)：TensorBoard 是可视化训练指标（包括损失、[accuracy](https://www.ultralytics.com/glossary/accuracy) 等）的热门选择。你可以将其与你的 YOLO26 训练过程集成。
  * [Comet](https://bit.ly/yolov8-readme-comet)：Comet 提供了一套用于实验跟踪和对比的综合工具包。它允许你跟踪指标、超参数甚至模型权重。与 YOLO 模型的集成也非常简单，能为你提供实验周期的全面概览。
  * [Ultralytics Platform](https://platform.ultralytics.com/): Ultralytics Platform 提供了一个用于跟踪 YOLO 模型的专业环境，为你提供了一个管理指标、数据集甚至与团队协作的一站式平台。鉴于其对 YOLO 的量身定制，它提供了更多自定义的跟踪选项。



这些工具各有优势，因此在做出选择时，你可能需要考虑项目的特定需求。

#### Link to this section如何检查训练是否在 GPU 上进行#

**问题** ：训练日志中的 'device' 值为 'null'，你不确定训练是否正在 GPU 上进行。

**解决方案** ：'device' 值为 'null' 通常意味着训练过程被设置为自动使用可用的 GPU，这是默认行为。为确保训练在特定的 GPU 上进行，你可以手动将 .yaml 配置文件中的 'device' 值设置为 GPU 索引（例如，'0' 代表第一个 GPU）：
    
    
    device: 0

这将明确地将训练过程分配给指定的 GPU。如果你希望在 CPU 上训练，请将 'device' 设置为 'cpu'。

密切关注 'runs' 文件夹中的日志和指标，以有效地监控训练进度。

#### Link to this section模型有效训练的关键注意事项#

如果你在模型训练相关问题上遇到困难，这里有一些需要牢记的事项。

**数据集格式与标签**

  * 重要性：任何 [machine learning](https://www.ultralytics.com/glossary/machine-learning-ml) 模型的基础都建立在它所训练数据的质量和格式上。
  * 建议：确保你的自定义数据集及其相关标签符合预期格式。验证标注是否准确且高质量至关重要。不正确或低质量的标注可能会破坏模型的学习过程，导致不可预知的结果。



**模型收敛**

  * 重要性：实现模型收敛可确保模型已从 [training data](https://www.ultralytics.com/glossary/training-data) 中充分学习。
  * 建议：在“从头开始”训练模型时，确保模型达到满意的收敛水平至关重要。与微调现有模型相比，这可能需要更长的训练时间和更多的 [epochs](https://www.ultralytics.com/glossary/epoch)。



**[Learning Rate](https://www.ultralytics.com/glossary/learning-rate) 与 Batch Size**

  * 重要性：这些超参数在确定模型训练期间如何更新权重方面起着关键作用。
  * 建议：定期评估所选的学习率和 batch size 是否针对你的特定数据集进行了优化。与数据集特征不匹配的参数可能会阻碍模型性能。



**类别分布**

  * 重要性：数据集中类别的分布会影响模型的预测倾向。
  * 建议：定期评估数据集中类别的分布。如果存在类别不平衡，模型就有倾向于多数类的风险。这种偏见可以在混淆矩阵（confusion matrix）中体现出来，模型可能主要预测多数类。



**与预训练权重进行交叉检查**

  * 重要性：利用预训练权重可以为模型训练提供一个良好的起点，特别是在数据有限的情况下。
  * 建议：作为诊断步骤，考虑使用相同数据训练模型，但使用预训练权重进行初始化。如果此方法产生了良好的混淆矩阵，则可能暗示“从头开始”训练的模型需要进一步训练或调整。



### Link to this section模型预测相关问题#

本节将解决模型预测期间遇到的常见问题。

#### Link to this section使用 YOLO26 自定义模型获取边界框预测#

**问题** ：在使用自定义 YOLO26 模型进行预测时，边界框坐标的格式和可视化存在挑战。

**解决方案** ：

  * 坐标格式：YOLO26 以绝对像素值提供边界框坐标。要将其转换为相对坐标（范围从 0 到 1），你需要除以图像尺寸。例如，假设你的图像大小为 640x640，那么你将执行以下操作：
        
        # Convert absolute coordinates to relative coordinates
        x1 = x1 / 640  # Divide x-coordinates by image width
        x2 = x2 / 640
        y1 = y1 / 640  # Divide y-coordinates by image height
        y2 = y2 / 640

  * 文件名：要获取你正在预测的图像文件名，请直接从预测循环内的结果对象中访问图像文件路径。




#### Link to this section在 YOLO26 预测中过滤对象#

**问题** ：在使用 Ultralytics 库运行 YOLO26 时，遇到如何过滤并在预测结果中仅显示特定对象的问题。

**解决方案** ：要检测特定类别，请使用 classes 参数指定你希望包含在输出中的类别。例如，仅检测汽车（假设 'cars' 的类别索引为 2）：
    
    
    yolo task=segment mode=predict model=yolo26n-seg.pt source='path/to/car.mp4' show=True classes=2

#### Link to this section理解 YOLO26 中的精确度指标#

**问题** ：对 YOLO26 中边界框精确度（box precision）、掩码精确度（mask precision）和 [confusion matrix](https://www.ultralytics.com/glossary/confusion-matrix) 精确度之间的区别感到困惑。

**解决方案** ：边界框精确度使用 IoU（交并比）作为指标，衡量预测的边界框与实际真值框的准确性。掩码精确度评估像素级对象分类中预测分割掩码与真值掩码的一致性。另一方面，混淆矩阵精确度侧重于所有类别的总体分类准确性，而不考虑预测的几何准确性。需要注意的是，即使类别预测错误，[bounding box](https://www.ultralytics.com/glossary/bounding-box) 在几何上仍可能是准确的（真阳性），这导致了边界框精确度和混淆矩阵精确度之间的差异。这些指标评估了模型性能的不同侧面，反映了在不同任务中对不同评估指标的需求。

#### Link to this section提取 YOLO26 中的对象尺寸#

**问题** ：在 YOLO26 中获取检测对象的长度和高度有困难，尤其是当图像中检测到多个对象时。

**解决方案** ：要检索边界框尺寸，首先使用 Ultralytics YOLO26 模型预测图像中的对象。然后，从预测结果中提取边界框的宽度和高度信息。
    
    
    from ultralytics import YOLO
    
    # Load a pretrained YOLO26 model
    model = YOLO("yolo26n.pt")
    
    # Specify the source image
    source = "https://ultralytics.com/images/bus.jpg"
    
    # Make predictions
    results = model.predict(source, save=True, imgsz=320, conf=0.25)
    
    # Extract bounding box dimensions
    boxes = results[0].boxes.xywh.cpu()
    for box in boxes:
        x, y, w, h = box
        print(f"Width of Box: {w}, Height of Box: {h}")

### Link to this section部署挑战#

#### Link to this sectionGPU 部署问题#

**问题：** 在多 GPU 环境中部署模型有时会导致意外行为，例如意想不到的内存使用、跨 GPU 结果不一致等。

**解决方案：** 检查默认 GPU 初始化。一些框架（如 PyTorch）可能在转换到指定 GPU 之前在默认 GPU 上初始化 CUDA 操作。为了绕过意外的默认初始化，请在部署和预测过程中直接指定 GPU。然后，使用工具监控 GPU 利用率和内存使用情况，以实时识别任何异常。此外，确保你使用的是框架或库的最新版本。

#### Link to this section模型转换/导出问题#

**问题：** 在将机器学习模型转换或导出为不同格式或平台的过程中，用户可能会遇到错误或意外行为。

**解决方案：**

  * 兼容性检查：确保你使用的库和框架版本彼此兼容。版本不匹配可能导致转换过程中出现意外错误。
  * 环境重置：如果你使用的是 Jupyter 或 Colab 等交互式环境，请考虑在进行重大更改或安装后重新启动你的环境。重新开始有时可以解决潜在问题。
  * 官方文档：请始终查阅你正在使用的转换工具或库的官方文档。它通常包含有关模型导出的具体指南和最佳实践。
  * 社区支持：查看库或框架的官方仓库，了解其他用户报告的类似问题。维护者或社区成员可能已在讨论区提供了解决方案或替代方法。
  * 定期更新：确保你使用的是该工具或库的最新版本。开发人员经常发布更新来修复已知漏洞或改进功能。
  * 增量测试：在进行完整转换之前，使用较小的模型或数据集测试该过程，以便尽早发现潜在问题。



## Link to this section社区与支持#

与志同道合的社区成员互动可以极大地提升你在使用 YOLO26 时的体验和成功率。以下是一些你可能会觉得有帮助的渠道和资源。

### Link to this section获取帮助的论坛和渠道#

**GitHub Issues:** YOLO26 的 GitHub 仓库有一个 [Issues 标签页](https://github.com/ultralytics/ultralytics/issues)，你可以在这里提问、报告 Bug 以及建议新功能。社区成员和维护者在这里非常活跃，这是获取特定问题帮助的好地方。

**Ultralytics Discord 服务器：** Ultralytics 有一个 [Discord 服务器](https://discord.com/invite/ultralytics)，你可以在那里与其他用户和开发人员交流。

### Link to this section官方文档和资源#

**Ultralytics YOLO26 文档** ：[官方文档](/zh)提供了 YOLO26 的全面概述，以及安装、使用和故障排查指南。

这些资源应该能为你的 YOLO26 项目故障排查和改进打下坚实的基础，同时也能让你与 YOLO26 社区中的其他人建立联系。

## Link to this section结论#

故障排查是任何开发过程中不可或缺的一部分，掌握正确的知识可以显著减少解决问题所花费的时间和精力。本指南旨在解决 Ultralytics 生态系统中 YOLO26 模型用户最常遇到的挑战。通过理解和解决这些常见问题，你可以确保项目进展更顺利，并在你的 [计算机视觉](https://www.ultralytics.com/glossary/computer-vision-cv) 任务中获得更好的结果。

请记住，Ultralytics 社区是一项宝贵的资源。与同行开发者和专家互动可以提供标准文档中可能未涵盖的额外见解和解决方案。时刻保持学习、实验并分享你的经验，为社区的集体知识库做出贡献。

## Link to this section常见问题解答#

### Link to this section如何解决 YOLO26 的安装错误？#

安装错误通常是由兼容性问题或缺少依赖项引起的。请确保使用 Python 3.8 或更高版本，并安装了 PyTorch 1.8 或更高版本。使用虚拟环境来避免冲突是有益的。有关分步安装指南，请遵循我们的 [官方安装指南](/zh/quickstart)。如果你遇到导入错误，请尝试全新安装或将库更新到最新版本。

### Link to this section为什么我的 YOLO26 模型在单 GPU 上训练缓慢？#

在单 GPU 上进行训练可能因为批处理大小（batch size）过大或内存不足而变慢。为了加快训练速度，请使用多个 GPU。确保你的系统有多个 GPU 可用，并设置 `device` 参数，例如 `device=[0,1,2,3]`。相应地增加批处理大小以充分利用 GPU，同时不超过内存限制。示例命令：
    
    
    model.train(data="/path/to/your/data.yaml", batch=32, device=[0, 1, 2, 3])

### Link to this section我该如何确保我的 YOLO26 模型正在使用 GPU 进行训练？#

如果训练日志中的 'device' 值显示为 'null'，通常意味着训练过程被设置为自动使用可用的 GPU。要显式指定特定 GPU，请在你的 `.yaml` 配置文件中设置 'device' 值。例如：
    
    
    device: 0

这将把训练过程设置为在第一个 GPU 上运行。请查看 `nvidia-smi` 命令以确认你的 CUDA 设置。

### Link to this section我该如何监控和追踪我的 YOLO26 模型训练进度？#

通过 [TensorBoard](https://www.tensorflow.org/tensorboard)、[Comet](https://bit.ly/yolov8-readme-comet) 和 [Ultralytics Platform](https://platform.ultralytics.com/) 等工具，可以有效地管理和可视化训练进度。这些工具允许你记录和可视化指标，例如损失（loss）、[精度](https://www.ultralytics.com/glossary/precision)、[召回率](https://www.ultralytics.com/glossary/recall) 和 mAP。根据这些指标实施 提前停止 也有助于实现更好的训练结果。

### Link to this section如果 YOLO26 无法识别我的数据集格式，我该怎么办？#

请确保你的数据集和标签符合预期的格式。验证标注是否准确且高质量。如果你遇到任何问题，请参阅 [数据收集与标注](https://docs.ultralytics.com/guides/data-collection-and-annotation) 指南以获取最佳实践。有关更多针对数据集的指导，请查看文档中的 [数据集](https://docs.ultralytics.com/datasets) 部分。

贡献者

[](https://github.com/ultralytics/ultralytics/blob/main/docs/en/guides/yolo-common-issues.md)

[GLglenn-jocher14](https://github.com/glenn-jocher)[RIRizwanMunawar4](https://github.com/RizwanMunawar)[ARartest081](https://github.com/artest08)[PDpderrenger1](https://github.com/pderrenger)[AKakdanz1](https://github.com/akdanz)[Y-Y-T-G1](https://github.com/Y-T-G)WIWillie Maddox1[LALaughing-q1](https://github.com/Laughing-q)

已创建 2023年11月12日已更新 上周

## 评论

本页内容

使用 AI 探索
